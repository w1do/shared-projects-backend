# Tasks: content-cities-and-regions

## 1. Справочник и его синхронизация

- [x] 1.1 Миграции `regions`, `cities`, `project_cities` в `packages/cms/content/database/migrations/`: регион (название, федеральный округ), город (регион, название, население, координаты, слаг), включённость (проект, город, уникальность пары); проверить прогон миграций и откат
- [x] 1.2 Модели `Region`, `City` (общие, без `project_id`) и `ProjectCity` (`BelongsToProject`), слаг города через `spatie/laravel-sluggable`; проверить уникальность слага на городах-тёзках из разных регионов
- [x] 1.3 Исключение справочных таблиц в архитектурном гейте и инвентаре: `regions` и `cities` без `project_id` — осознанное исключение, а не пропуск; проверить `packages/cms/shared/tests/ArchitectureGateTest.php` и `./tools/refactor-inventory.sh --strict`
- [x] 1.4 Копия исходного набора данных о городах России в ресурсах пакета content с указанием происхождения (arbaev/russia-cities); проверить, что файл читается без сети
- [x] 1.5 Handler синхронизации в `Application/Handlers/`: upsert по паре «регион + название», обновление изменившихся полей, сбор счётчиков и расхождений; удаления нет; проверить два прогона подряд — второй показывает нулевые изменения
- [x] 1.6 Команда `city:sync` в `packages/cms/content/src/Console/` с аргументом источника (путь или URL) и сводкой прогона; проверить прогон из копии и из файла с изменённым населением
- [x] 1.7 `city:sync` в бутстрапе стека (`tools/cms`) — без справочника раздел пуст; проверить `./tools/cms bootstrap` на чистом стеке

## 2. Состав городов проекта

- [x] 2.1 Стартовый набор: при первом обращении проекта к городам создаются записи включённости для 10 крупнейших по населению, повторно не применяется; проверить сценарий «выключил город из десятки → список запросил снова» — город остался выключенным
- [x] 2.2 Команды включения и выключения одного города; проверить изоляцию — состав проекта A не влияет на B
- [x] 2.3 Массовые действия «включить все» и «вернуться к 10 крупнейшим» одним запросом батч-операцией; проверить время и число запросов на полном справочнике
- [x] 2.4 Query списка городов проекта: поиск по названию, отбор по региону и включённости, сортировка по населению и названию, курсорная пагинация общим механизмом; проверить отклик на полном справочнике
- [x] 2.5 Права `content.cities.view` и `content.cities.manage` и пункт навигации `content.cities` в `ContentManifest`, версия манифеста поднята; проверить bootstrap консоли — пункт появился в группе «Контент»
- [x] 2.6 Маршруты раздела в `packages/cms/content/routes/admin.php` за гейтом сервиса `content` и правами; проверить 404 при выключенном сервисе и 403 без права управления

## 3. SEO города

- [x] 3.1 `SeoableType` пополняется значением `city`, модель города подключается к полиморфному SEO; проверить чтение и запись SEO города через существующие маршруты `/content/seo/{type}/{id}`
- [x] 3.2 SEO принадлежит паре «проект + город»: проверить, что два проекта хранят независимое SEO одного города
- [x] 3.3 Каталог SEO городами не пополняется — проверить, что `/content/seo` возвращает прежние типы сущностей
- [x] 3.4 Публичное API: список включённых городов проекта и город по слагу с SEO-блоком; проверить, что выключенные города в выдаче отсутствуют

## 4. AI-адаптация SEO городов

- [x] 4.1 Новая категория инструкции для SEO городов в `packages/cms/instructs` с предустановленной инструкцией платформы и схемой ответа; проверить, что при отсутствии своей инструкции применяется предустановленная
- [x] 4.2 Новый вид фоновой задачи в `packages/cms/shared/src/BackgroundTasks/BackgroundTaskKind.php`; проверить, что задача отдаётся списком задач проекта с видом и предметом
- [x] 4.3 Job адаптации: обход включённых городов пачками, ход задачи двигается по мере обработки, отказ по городу не затирает его SEO и не останавливает задачу; проверить на проекте с намеренно падающим ответом модели
- [x] 4.4 Запуск задачи: право `content.cities.manage`, тематика проекта с возможностью переопределения, отказ при пустой тематике, отклонение повторного запуска при выполняющейся задаче; проверить все три ветки
- [x] 4.5 `./tools/cms api` — новые маршруты в едином swagger

## 5. Консоль: раздел «Города»

- [x] 5.1 Раздел `frontends/admin/src/components/pages/cities` и маршрут `src/app/admin/cities`: таблица из склада `source-admin`, слой данных в `src/lib/admin`, хук раздела; проверить, что данные идут из платформы, моков нет
- [x] 5.2 Таблица: название, регион, население, переключатель включённости в строке, признак заполненности SEO; поиск, отбор по региону и включённости, сортировка; проверить в браузере на полном справочнике
- [x] 5.3 Массовые действия с подтверждением: «включить все», «вернуться к 10 крупнейшим»; проверить обновление таблицы без перезагрузки
- [x] 5.4 Диалог SEO города теми же полями, что у прочих сущностей, с показом ошибок платформы; без права управления — только просмотр; проверить обе ветки
- [x] 5.5 Диалог AI-адаптации: тематика, предзаполненная тематикой проекта, число затрагиваемых городов, запуск; проверить отказ при пустой тематике
- [x] 5.6 Прогресс задачи в разделе: задача видна сразу после запуска, ход обновляется, состояние переживает перезагрузку страницы, отказ показан человеческим текстом; проверить в браузере
- [x] 5.7 Пункт «Города» в сайдбаре в группе «Контент» — по сервису и праву; проверить исчезновение пункта при выключении сервиса без повторного входа
- [x] 5.8 Зеркалирование со складом и тексты `console.*` в реестре без осиротевших ключей; проверить сверку консоли со складом и реестр текстов

## 6. Приёмка

- [x] 6.1 Backend-проверки: Pint, PHPStan level 8, Pest content-сервиса, `./tools/refactor-inventory.sh --strict`
- [x] 6.2 Сценарий целиком на поднятом стеке: `city:sync` → раздел открывается с 10 включёнными городами → «включить все» → AI-адаптация с прогрессом → SEO городов заполнено → публичное API отдаёт включённые города с SEO; зафиксировать в e2e панели
- [x] 6.3 Объёмы и время: прогон `city:sync`, отклик списка и время массового включения на полном справочнике — зафиксировать фактические значения
