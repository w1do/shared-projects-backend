## 1. Зеркало: снять симметричную сверку

- [x] 1.1 Написать `tools/mirror-check.sh`: сверяет только файлы, присутствующие и в `frontends/source-admin`, и в `frontends/admin` (по путям `src/components src/app src/styles.css src/theme.css public`); расхождение содержимого — ошибка, односторонний файл — норма. Проверить: скрипт зелёный на текущем состоянии, красный после правки любого перенесённого файла в `admin`
- [x] 1.2 Заменить шаг `Layout matches source-admin` в `.github/workflows/pull-request.yml` на вызов `tools/mirror-check.sh`. Проверить: `bash tools/mirror-check.sh` проходит локально
- [x] 1.3 Написать `tools/mirror-own-files.sh`: печатает файлы `frontends/admin`, которых нет в складе, — список для линта. Проверить: вывод не содержит перенесённых файлов и содержит `src/lib/admin/data-source/*`

## 2. Удаление разделов без сервиса платформы

- [x] 2.1 Удалить раздел «Товары»: `src/app/admin/products/**`, `src/components/pages/products/**`, `src/lib/admin/products/**`, `src/lib/admin/mocks/products.ts`, `src/lib/admin/products-helpers.ts`, его запросы в `data-source/queries/catalog.ts`, мутации в `data-source/mutations/catalog-products.ts`, маппер `mappers/catalog-products.ts`, ключ в `section-access.ts`, пункт в `sidebar-config.ts`, тексты `console.products.*`. Проверить: `bun run build` зелёный
- [x] 2.2 Удалить раздел «Варианты» (`variants`) со всеми его файлами, ключами и текстами. Проверить: `bun run build` зелёный
- [x] 2.3 Удалить раздел «Бренды» (`brands`, включая `brand-mocks.ts` и `mocks/brands/`). Проверить: `bun run build` зелёный
- [x] 2.4 Удалить раздел «Коллекции» (`collections`). Проверить: `bun run build` зелёный
- [x] 2.5 Удалить раздел «Склад» (`inventory`, включая `inventory-import-helpers.ts` и `mocks/inventory.ts`). Проверить: `bun run build` зелёный
- [x] 2.6 Удалить раздел «Заказы» (`orders`), его запросы в `data-source/queries/commerce.ts` и маппер `mappers/commerce.ts`. Проверить: `bun run build` зелёный
- [x] 2.7 Удалить раздел «Кампании» (`campaigns`, включая `mocks/campaigns-data.ts`). Проверить: `bun run build` зелёный
- [x] 2.8 Удалить раздел «Акции» (`promotions`). Проверить: `bun run build` зелёный
- [x] 2.9 Удалить раздел «Поддержка» (`support`). Проверить: `bun run build` зелёный
- [x] 2.10 Удалить раздел «Уведомления» (`notifications`) вместе с `NotificationsBell` в верхней панели и секцией уведомлений в настройках, если она осталась без API. Проверить: `bun run build` зелёный, панель рендерится без колокольчика
- [x] 2.11 Убрать из дашборда виджеты удалённых разделов (`BestSellers`, `BrandPerformance`, `CampaignPerformance`, `CategorySales`, `LowStock`, `RecentOrders`) — их данных в платформе нет. Проверить: дашборд собирается и показывает только виджеты, подключённые к analytics/content
- [x] 2.12 Сократить `CONSOLE_SECTION_KEYS` и `SECTION_REQUIREMENTS` в `section-access.ts` до девяти подключённых разделов; обновить `section-access.test.ts`. Проверить: `bun test` зелёный
- [x] 2.13 Удалить из реестра `console-texts.ts` ключи удалённых разделов; обновить `console-texts.test.ts`. Проверить: `bun test` зелёный, дублей и осиротевших ключей нет
- [x] 2.14 Проверить, что адреса удалённых разделов отвечают 404: открыть `/admin/products` и `/admin/orders` на собранном бандле

## 3. Убрать режим демо-данных

- [ ] 3.1 Заменить оставшиеся вызовы `fromSource(apiFn, mockValue)` на прямой вызов API в `data-source/queries/*`. Проверить: `grep -r "fromSource" frontends/admin/src` ничего не находит
- [ ] 3.2 Удалить `getAdminDataSource`, `shouldUseAdminApi`, `getAdminMockDelayMs` из `data-source/config.ts` и все ветвления по ним. Проверить: `grep -rn "shouldUseAdminApi\|NEXT_PUBLIC_ADMIN_DATA_SOURCE\|ADMIN_MOCK_DELAY" frontends/admin` ничего не находит
- [ ] 3.3 Удалить `src/lib/admin/mock.ts` и каталог `src/lib/admin/mocks/**` целиком; для подключённых разделов, где данные приходили из демо-набора, подставить API платформы. Проверить: `bun run build` зелёный, ни один импорт не ведёт в `mocks/`
- [ ] 3.4 Убрать переменные демо-режима из `frontends/admin/.env*`, `infra/compose/*.yaml` и документации запуска. Проверить: стек поднимается `./tools/cms up` и консоль читает платформу
- [ ] 3.5 Обновить дельту `openspec/specs`-зависимых снимков и e2e, если они опирались на демо-данные. Проверить: `npm test` в `e2e` зелёный

## 4. Реестр фоновых задач на платформе

- [ ] 4.1 Добавить в `packages/cms/shared/src/BackgroundTasks/` модель `BackgroundTask` (плоский namespace `Cms\Shared\BackgroundTasks`) с полями: `project_id`, `kind`, `state`, `stage`, `subject_type`, `subject_id`, `initiated_by`, `failure_reason`, `queued_at`, `started_at`, `finished_at`. Проверить: архитектурный гейт зелёный (`shared` — библиотека, four-layer к ней не применяется)
- [ ] 4.2 Добавить миграцию `packages/cms/shared/database/migrations/` для таблицы `background_tasks` и загрузку миграций в `SharedServiceProvider`. Проверить: `./tools/cms migrate content-service` создаёт таблицу
- [ ] 4.3 Добавить enum состояний (`queued`, `running`, `succeeded`, `failed`) с переходами в методах enum. Проверить: Pest-тест запрещает переход из конечного состояния
- [ ] 4.4 Добавить порт `TaskProgress` (`start`, `stage`, `succeed`, `fail`) и его реализацию поверх модели; зарегистрировать в `SharedServiceProvider`. Проверить: Pest-тест пишет ход через порт и читает результат из таблицы
- [ ] 4.5 Изолировать реестр по проекту: `BelongsToProject` на модели, запрос чужой задачи отвечает как несуществующая. Проверить: Pest-тест на изоляцию (И11 — 404, не 403)
- [ ] 4.6 Скрыть технические детали отказа: наружу уходит сообщение доменного исключения или родовая фраза, класс и стек — только в лог. Проверить: Pest-тест на отказ с `RuntimeException` не содержит имени класса в `failure_reason`

## 5. Фоновые операции сообщают ход

- [ ] 5.1 Заводить запись задачи в handler'е команды до `dispatch()`, передавать её идентификатор в job. Проверить: Pest-тест — после вызова handler'а с отложенной очередью запись существует в состоянии `queued`
- [ ] 5.2 Подключить `GeneratePostJob`: этапы (подготовка темы → запрос к ИИ → сборка блоков → сохранение поста), успех со ссылкой на созданный пост, `failed()` пишет причину. Проверить: Pest-тест проходит job через реальную очередь и читает конечное состояние
- [ ] 5.3 Подключить `ProcessResearchJob` и `IndexResearchJob`: этапы исследования, согласованность с собственным статусом исследования. Проверить: Pest-тест — завершённое исследование не оставляет задачу в состоянии `running`
- [ ] 5.4 Подключить `BuildProjectJob`: этапы сборки проекта по AI. Проверить: Pest-тест на смену этапов
- [ ] 5.5 Перевести импорт медиа по ссылке в очередь и подключить к реестру (сейчас он синхронный, а скачивание внешнего файла — долгая операция). Проверить: `MediaImportTest` зелёный, задача видна в реестре
- [ ] 5.6 Добавить в планировщик регулярную чистку завершённых задач старше срока выдачи. Проверить: Pest-тест — старая завершённая задача удаляется, свежая остаётся

## 6. Admin API задач

- [ ] 6.1 Добавить `Presentation/Http/Api/V1` в `shared`-совместимом виде: контроллер списка задач проекта, `FormRequest` фильтра, `JsonResource` задачи; маршрут в `routes/internal.php` или в `content` admin-роутах — по месту, где живут задачи. Проверить: `./tools/cms api` пересобирает swagger без ошибок
- [ ] 6.2 Отдавать сначала выполняющиеся, затем недавно завершённые; пустой проект получает пустой список, а не ошибку. Проверить: Pest-тест порядка и пустого списка
- [ ] 6.3 Закрыть маршрут правом `<service>.tasks.view` и добавить его в манифест сервиса. Проверить: Pest-тест — оператор без права получает 403
- [ ] 6.4 Добавить характеризационный снимок ответа и запись в `RouteCoverageTest`. Проверить: `RouteCoverageTest` зелёный
- [ ] 6.5 Пересобрать `openapi/` и сгенерированные типы консоли. Проверить: CI-шаг актуальности swagger зелёный

## 7. Прогресс в консоли

- [ ] 7.1 Добавить запрос списка задач в `data-source` с опросом: частым, пока есть выполняющиеся, и остановленным в покое. Проверить: unit-тест на выбор интервала по составу списка
- [ ] 7.2 Добавить индикатор в `AdminTopbar`: число выполняющихся задач; при пустом списке индикатор не рендерится; ошибка запроса не ломает панель. Проверить: unit-тест на три состояния (есть работа / нет работы / запрос упал)
- [ ] 7.3 Добавить список задач по клику: вид работы, предмет, этап, состояние; тексты — в реестре `console.tasks.*`. Проверить: `bun test` зелёный, ключи присутствуют в реестре
- [ ] 7.4 Показывать состояние задачи по месту: у темы в разделе «Ресёрч» — ход написания поста; кнопка запуска недоступна, пока задача по этой теме выполняется. Проверить: e2e-сценарий — после нажатия «Написать пост» тема сразу показывает работу, кнопка неактивна
- [ ] 7.5 Показывать отказ с причиной и оставлять предмет доступным для повторного запуска. Проверить: e2e-сценарий с заведомо падающей задачей показывает причину, а не пустой экран
- [ ] 7.6 Проверить, что перезагрузка страницы во время работы возвращает текущее состояние. Проверить: e2e-сценарий с перезагрузкой

## 8. Версия кода и перезапуск обработчиков

- [ ] 8.1 Прокинуть версию сборки в образ (аргумент сборки → переменная окружения) в общем Dockerfile и `infra/compose/compose.yaml`. Проверить: `docker compose config` показывает переменную у сервисов и воркеров
- [ ] 8.2 Отдавать версию в `/health` всех четырёх сервисов; отсутствие значения — `unknown`, а не ошибка старта. Проверить: `curl` каждого `/health` возвращает поле версии
- [ ] 8.3 Отдавать версию воркеров и планировщика тем же способом (команда или лог старта), чтобы расхождение было видно. Проверить: расхождение версий сервиса и воркера заметно без чтения логов приложения
- [ ] 8.4 Добавить в `tools/cms` выкат, который поднимает сервисы вместе с их воркерами и планировщиками одной операцией. Проверить: после выката `docker compose ps` показывает свежее время старта у `*-worker` и `*-scheduler`
- [ ] 8.5 Обновить `docs/` по запуску и выкату под новую команду. Проверить: описанная последовательность выполняется с нуля

## 9. Линт консоли и проверки

- [ ] 9.1 Настроить линт консоли так, чтобы он получал список файлов из `tools/mirror-own-files.sh`. Проверить: запуск не трогает перенесённые файлы (`tools/mirror-check.sh` после него зелёный)
- [ ] 9.2 Исправить нарушения в собственных файлах консоли до зелёного. Проверить: линт консоли завершается без ошибок
- [ ] 9.3 Добавить шаг линта консоли в `.github/workflows/pull-request.yml`. Проверить: шаг зелёный на текущей ветке
- [ ] 9.4 Добавить шаг Pint в backend-job, если его там нет. Проверить: `composer lint` зелёный в корне
- [ ] 9.5 Прогнать полный набор: `composer lint`, `composer stan`, `composer test`, `./tools/refactor-inventory.sh --strict`, `./tools/cms api`, `bun run build`, `bun test`, e2e. Проверить: все зелёные
