## 1. Ревизии: узнаваемая версия и удаление

- [ ] 1.1 Добавить в `RevisionDTO` поля `number` (порядковый номер версии поста от 1 по возрастанию `created_at`) и `title` (из `snapshot['title']`, при отсутствии — пусто); заполнять их в `ListRevisionsQuery`. Проверка: тест пакета content — у поста с тремя сохранениями версии пронумерованы 1–3, заголовки соответствуют снимкам.
- [ ] 1.2 Добавить команду `DeleteRevisionCommand` и `DeleteRevisionHandler`: удаление ревизии с проверкой принадлежности посту и текущему проекту, чужая ревизия — доменное исключение `ContentRuleViolation`. Проверка: тесты — своя ревизия удаляется, ревизия другого поста и другого проекта отклоняются, пост не меняется.
- [ ] 1.3 Объявить маршрут `DELETE api/admin/v1/projects/{project}/content/posts/{post}/revisions/{revision}` под правом `content.posts.manage`, контроллерный метод — тонкий (Request → Handler → ответ). Проверка: тесты — 204 для своей ревизии, 403 без права, 422/404 для чужой.
- [ ] 1.4 Обновить снимок контракта `posts-revisions` (новые поля `number`, `title`) и пересобрать `openapi/openapi.json` командой `./tools/cms api`. Проверка: `php artisan test` зелёный, `git diff --exit-code openapi/` после пересборки пуст.
- [ ] 1.5 Консоль: в `PostLifecycleCard` показывать «Версия N · заголовок · дата» вместо `#{id}` и добавить действие удаления версии с подтверждением и сообщением об исходе. Проверка: раздел открыт в браузере — список версий читаем, удаление убирает строку, пост не меняется.

## 2. Закрепление поста сверху

- [ ] 2.1 Миграция: `posts.is_featured` (boolean, default false) и частичный уникальный индекс по `project_id` при `is_featured`. Проверка: `./tools/cms migrate` проходит, повторная установка признака второму посту проекта отклоняется базой.
- [ ] 2.2 Провести `is_featured` через `Post`, `UpsertPostDTO`, `UpsertPostRequest`, `PostDTO` и `PostResource`. Проверка: тест — сохранение поста с признаком возвращает его в ответе и переживает повторное чтение.
- [ ] 2.3 В `UpsertPostHandler` снимать признак с прежнего закреплённого поста проекта в одной транзакции с установкой нового. Проверка: тест — после закрепления второго поста закреплён ровно один, первый признак потерял.
- [ ] 2.4 Обновить снимки контрактов `posts-index`, `posts-show` и пересобрать swagger. Проверка: `php artisan test` зелёный, `git diff --exit-code openapi/` пуст.
- [ ] 2.5 Консоль: переключатель закрепления в форме поста; `use-blogs-page` выбирает закреплённый пост по признаку, а не первым из списка, и исключает его из общего списка. Проверка: в браузере закрепление поста поднимает его карточкой над списком, снятие — возвращает в список.
- [ ] 2.6 Починить показ изображения в карточке закреплённого поста: `next.config.ts` собирает `images.remotePatterns` из адреса платформы (dev — `localhost:8080`), демо-хост `images.unsplash.com` сохраняется. Проверка: в браузере запрос `/_next/image` для медиа платформы отвечает 200 и изображение видно; при пустой обложке показана заглушка.

## 3. Пересборка поста через AI

- [ ] 3.1 Вынести из `GeneratePostFromTopicHandler` общую сборку черновика (инструкция + материалы → заголовок, блоки, SEO) в Action уровня Application. Проверка: тесты генерации по теме остаются зелёными без изменения ожиданий.
- [ ] 3.2 Добавить `BackgroundTaskKind::PostRebuild` и подпись вида задачи в реестре текстов консоли. Проверка: тест реестра задач — задача пересборки попадает в выдачу задач проекта с предметом `post` и его id.
- [ ] 3.3 Добавить команду `RebuildPostCommand`, handler постановки задачи и job пересборки: материалы ищутся по заголовку и блокам поста, берутся первые десять блоков, сохранение идёт через `UpsertPostHandler` и `UpsertSeoHandler`, slug/категории/теги/изображения/статус не трогаются. Проверка: тесты — после пересборки изменились заголовок, блоки и SEO; slug, категории, теги, изображения и статус прежние; в истории появилась версия с прежним содержимым.
- [ ] 3.4 Ход задачи: `queue → start → stage → succeed/fail`, отказ оставляет пост неизменным и пишет причину в реестр. Проверка: тест — при отказе AI пост не изменился, задача отмечена отклонённой с человекочитаемой причиной.
- [ ] 3.5 Объявить маршрут `POST .../content/posts/{post}/rebuild` в `packages/cms/research/routes/admin.php` под правом `content.posts.manage`; пересобрать swagger. Проверка: тесты — 202/200 для уполномоченного оператора, 403 без права; `git diff --exit-code openapi/` пуст.
- [ ] 3.6 Консоль: действие «Пересобрать через AI» в форме поста с подтверждением, показом хода задачи и обновлением формы по завершении. Проверка: в браузере запуск показывает прогресс, по завершении форма содержит новый текст, ошибка показывается сообщением.

## 4. Просмотр статьи

- [ ] 4.1 `BlogPreviewModal`: собирать содержимое из блоков поста и рендерить markdown существующим `MarkdownEditor` в режиме только чтения. Проверка: в браузере просмотр поста из нескольких блоков показывает все блоки в исходном порядке с применённой разметкой.
- [ ] 4.2 Растянуть содержимое на всю ширину окна (убрать `max-w-prose`) и удалить блок автора вместе с сопутствующими данными. Проверка: в браузере содержимое занимает всю ширину, сведений об авторе нет.
- [ ] 4.3 Перенести изменения общих файлов вёрстки в `frontends/source-admin`. Проверка: `./tools/mirror-check.sh` без расхождений.

## 5. Пагинация как общий механизм

- [ ] 5.1 Добавить хук `usePagination` (страница, размер страницы, срез, сброс на первую при смене входных данных и при выходе за границы). Проверка: node-тесты хука — смена фильтра возвращает на первую страницу, уменьшение числа записей не оставляет на пустой.
- [ ] 5.2 Перевести на него разделы блогов, категорий и клиентов, удалив их копии состояния. Проверка: в браузере все три раздела листаются одинаково; `grep` не находит собственных `currentPage`/`itemsPerPage` в этих разделах.
- [ ] 5.3 Перенести изменения общих компонентов подвала и пагинации в `frontends/source-admin`. Проверка: `./tools/mirror-check.sh` без расхождений.

## 6. Сайдбар на мобильном

- [ ] 6.1 Закрывать мобильную панель немедленно при выборе пункта и подсвечивать активный пункт по нажатию, не дожидаясь конца анимации. Проверка: в браузере в мобильной ширине переход в загруженный раздел показывает его сразу, панель не перекрывает содержимое.
- [ ] 6.2 Привести длительность анимации сайдбара к общей для консоли и добавить уважение `prefers-reduced-motion`. Проверка: при включённой системной настройке уменьшенной анимации панель открывается и закрывается без движения.
- [ ] 6.3 Перенести изменения `ui/navigation/sidebar.tsx` в `frontends/source-admin`. Проверка: `./tools/mirror-check.sh` без расхождений.

## 7. Настройки → Основные

- [ ] 7.1 Добавить enum `ProjectType` (`blog`, `shop`, `corporate`, `landing`) и поля `project_type`, `timezone` в `SiteSettings` со значениями по умолчанию `blog` и `Europe/Moscow`. Проверка: тест — новый проект читает настройки со значениями по умолчанию.
- [ ] 7.2 Провести новые поля через `SiteSettingsDTO`, `UpdateSiteSettingsRequest` (тип — из enum, часовой пояс — из зон России, валюты — RUB/USD, язык — из локалей проекта) и `SiteSettingsResource`; отдавать вместе с настройками перечни допустимых значений и локали проекта. Проверка: тесты — недопустимые тип, пояс, валюта и язык вне локалей проекта отклоняются с 422, настройки не меняются.
- [ ] 7.3 Обновить снимок контракта настроек сайта и пересобрать swagger. Проверка: `php artisan test` зелёный, `git diff --exit-code openapi/` пуст.
- [ ] 7.4 Консоль: переписать `GeneralSection` под проект — заголовок «Главные настройки сайта», описание «Настройки сайта, управление данными», поля названия и описания (сохраняются в проект), тип проекта, часовой пояс, валюты и валюта по умолчанию, язык по умолчанию со списком языков проекта. Проверка: в браузере сохранение переживает перезагрузку, переименование видно в переключателе проектов.
- [ ] 7.5 Удалить из раздела поля склада вёрстки (единица веса, email поддержки, телефон, адрес витрины), заглушку `saveSettingsMutation` и запись `storefront_live_url` в `localStorage`; убрать осиротевшие опции и тексты. Проверка: `grep` не находит `storefront_live_url` и `WEIGHT_UNIT_OPTIONS` в `frontends/admin/src`; сборка панели проходит.
- [ ] 7.6 Свести язык по умолчанию в «Основные»: убрать отдельную секцию `SiteLanguageSection` со вкладки, сохранив управление языками проекта во вкладке «Языки». Проверка: в браузере вкладка «Основные» содержит выбор языка, вкладка «Языки» — список локалей проекта без дублирования.

## 8. Проверки перед завершением

- [ ] 8.1 Прогнать `./vendor/bin/pint --test ../../packages/cms app` и `./vendor/bin/phpstan analyse --memory-limit=1G` в затронутых сервисах. Проверка: оба шага зелёные без правок baseline и без `@phpstan-ignore`.
- [ ] 8.2 Прогнать `./tools/cms test` и `./tools/refactor-inventory.sh --strict`. Проверка: все четыре набора тестов зелёные, инвентарь слоёв без нарушений.
- [ ] 8.3 Перезапустить `content-service`, `content-worker`, `content-scheduler`, `auth-service`, `auth-worker` и `admin-front`, дождавшись сборки панели. Проверка: `/health/*` отвечают 200, консоль открывает раздел блогов и настройки без ошибок.
- [ ] 8.4 Пройти сценарии из `specs/` вручную в браузере: ревизии, закрепление, пересборка, просмотр, пагинация, сайдбар на мобильной ширине, настройки. Проверка: каждый сценарий воспроизведён, расхождения с требованиями отсутствуют.
