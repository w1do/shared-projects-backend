# Tasks: external-project-snapshot

## 1. Схема и хранение снапшота

- [ ] 1.1 Описать схему `schema_version: 1` как DTO (`spatie/laravel-data`) в `Application/DTOs/Snapshot/`: корень, `api`, `permissions[]`, `sections[]`, `view` типов `table` и `external`, колонки с перечнем типов из `design.md`; проверить юнит-тестом, что эталонный пример из `design.md` разбирается без ошибок
- [ ] 1.2 Реализовать правила валидации: неизвестная `schema_version`, дубли `sections[].key`, ссылка `section.permission` на необъявленное право, абсолютный `endpoint`, хост вне `api.allowed_hosts`, приватный диапазон адресов; проверить тестом, что каждый случай отклоняется и ошибки собираются списком, а не по первой
- [ ] 1.3 Добавить миграцию `project_snapshots` (`project_id`, `schema_version`, `payload` jsonb, `checksum`, `uploaded_by`, `created_at`) и ссылку `projects.active_snapshot_id`; проверить, что миграция накатывается и откатывается на чистой базе
- [ ] 1.4 Реализовать команду загрузки снапшота (валидация → новая версия → активация → `BootstrapCache::bump()` → аудит); проверить тестом: валидный документ создаёт версию и становится активным, невалидный не меняет активную версию

## 2. Права и участие в bootstrap

- [ ] 2.1 Регистрировать права снапшота на проект с префиксом `ext.<project-key>.`, проставляемым платформой; проверить тестом, что право из файла с именем `auth.settings.manage` сохраняется как `ext.<key>.auth.settings.manage` и платформенного доступа не даёт
- [ ] 2.2 Транслировать активный снапшот в `ServiceManifest` с ключом `ext:<project-key>` и включать его в `BuildBootstrap` рядом с манифестами сервисов; проверить тестом: разделы снапшота приходят в `bootstrap.services[]` и фильтруются по правам оператора тем же механизмом
- [ ] 2.3 Проверить тестом изоляцию проектом: у проекта без снапшота разделы другого проекта в `bootstrap` отсутствуют
- [ ] 2.4 Реализовать историю версий и откат (список версий с автором и датой, активация сохранённой версии со сбросом кэша и аудитом); проверить тестом, что после отката состав разделов соответствует выбранной версии

## 3. Прокси внешних вызовов

- [ ] 3.1 Реализовать эндпоинт данных раздела `GET /projects/{project}/snapshot/sections/{section}/data`, принимающий ключ раздела и параметры страницы/поиска, но не URL; проверить тестом, что адрес собирается из снапшота, а переданный клиентом адрес игнорируется
- [ ] 3.2 Подставлять учётные данные из настроек проекта по `api.auth.secret_ref` на стороне платформы; проверить тестом, что секрет не попадает в ответ клиенту
- [ ] 3.3 Проверять хост по белому списку повторно при выполнении запроса, после разрешения DNS, и отклонять приватные диапазоны; проверить тестом на имени, разрешающемся во внутренний адрес, — запрос не выполняется и событие пишется в журнал
- [ ] 3.4 Ограничить время ожидания и размер ответа внешнего API, приводить сбои к понятному коду ошибки; проверить тестом на недоступном и на слишком большом ответе
- [ ] 3.5 Отличать «ответ не содержит `rows_path`» от пустого списка и отдавать это разной ошибкой; проверить тестом на ответе неожиданного вида

## 4. Отрисовка разделов в панели

- [ ] 4.1 Расширить `section-access.ts` ключами `ext:*`: требование берётся из ответа `bootstrap`, а не из статической карты; проверить юнит-тестом, что раздел снапшота виден только с объявленным правом и попадает в снимок `console_sections`
- [ ] 4.2 Добавить маршрут `app/admin/ext/[project]/[section]` и зеркалить его в `frontends/source-admin`; проверить `diff -r` источника и панели — расхождений нет
- [ ] 4.3 Реализовать рендерер `table` поверх штатного `DataGrid`: объявленные колонки в объявленном порядке, форматирование по типам, `money` из минорных единиц, отсутствующее поле — пустая ячейка; проверить юнит-тестами на ответе с лишними и с отсутствующими полями
- [ ] 4.4 Реализовать постраничный просмотр и поиск по объявленным правилам; проверить, что при отсутствии объявления элементы управления не отображаются, а при наличии — запрашивается следующая страница
- [ ] 4.5 Реализовать рендерер `external` с явным обозначением перехода во внешнюю систему; проверить, что раздел не выдаёт чужой интерфейс за интерфейс панели
- [ ] 4.6 Показывать ошибку внешнего API внутри раздела, сохраняя работоспособность остальной панели; проверить на выключенном внешнем API, что навигация и другие разделы работают
- [ ] 4.7 Закрыть прямой переход на раздел снапшота без права через `src/proxy.ts` (снимок `console_sections`); проверить прямым переходом — редирект на `/admin/unauthorized`

## 5. Загрузка снапшота из панели

- [ ] 5.1 Добавить в настройки проекта экран загрузки снапшота с показом ошибок валидации построчно; проверить загрузкой заведомо невалидного файла, что перечислены все ошибки, а активная версия не изменилась
- [ ] 5.2 Добавить историю версий с автором, датой и действием «вернуть версию»; проверить откатом, что состав меню меняется после повторного входа
- [ ] 5.3 Добавить ввод значения для `api.auth.secret_ref` через механизм настроек проекта с маскированием; проверить, что значение не отдаётся обратно в открытом виде
- [ ] 5.4 Зеркалить новые экраны в `frontends/source-admin`; проверить `diff -r` по `src/components` и `src/app` — расхождений нет

## 6. Документация и сквозная проверка

- [ ] 6.1 Описать в `docs/admin-console.md` схему снапшота, перечень типов колонок, правила прокси и порядок загрузки/отката; проверить, что эталонный пример из документации проходит валидацию
- [ ] 6.2 Сквозная проверка через gateway на тестовом внешнем API: загрузка снапшота → раздел появился в меню → таблица отрисована по декларации с пагинацией и поиском → оператор без права раздела не видит → прямой переход даёт отказ → откат версии убирает раздел
- [ ] 6.3 Проверить, что проект без снапшота ведёт себя как раньше: состав меню, `bootstrap` и guard не изменились
