# Tasks — fix-known-behavioral-defects

Режим противоположен рефакторингу: каждая задача МЕНЯЕТ наблюдаемое поведение. Правило: обновление снимка/guard-теста легально только внутри своей задачи и фиксируется в отметке выполнения строкой «обновлён тест X: было …, стало …». Все прочие снимки обязаны остаться нетронутыми; финальная сверка — задача 4.2.

Гейт каждой задачи: pest затронутого сервиса зелёный; финальный гейт — все четыре сервиса + pint + stan + `./tools/refactor-inventory.sh --strict` + архитектурный гейт.

## 1. Малые изоляты

- [x] 1.1 (Д1) Удалить мёртвый `CachedIntrospector::forgetToken()` (ключ никогда не совпадал с ключом записи, вызывающих нет) и его юнит-тест; в докблоке класса зафиксировать: единственный механизм инвалидации — полный flush через `/internal/cache-bust`. Проверить: grep `forgetToken` по репо — 0; тесты shared зелёные во всех сервисах.
  - Сделано: метод и докблок-ловушка удалены (вызывающих было 0, юнит-теста на него не оказалось); в докблоке класса зафиксировано, что единственный механизм инвалидации — cache-bust. grep = 0.
- [x] 1.2 (Д8) Удалить мёртвый `$order` из `ai/src/Infrastructure/Ai/StructuredResponseMapper.php:80-106`; проверить тестами ai (поведение buildTree не меняется).
  - Сделано: `$order` удалён из `categoryTree()`; тесты ai зелёные.
- [x] 1.3 (Д6) Удалить `AdminPermissionResolver::withTeamThenClear()`; `CreateProjectHandler` и `ResolveProject` перевести на `withTeam()` (восстановление предыдущего значения — вложенные свопы больше не затирают контекст). Проверить: полный pest auth зелёный (в HTTP-запросе предыдущее значение null — наблюдаемых изменений нет), guard 0.12 зелёный.
  - Сделано: оба потребителя на `withTeam()` (восстановление предыдущего значения), метод и его докблок удалены; полный pest auth 279 зелёный, guard 0.12 в составе.
- [x] 1.4 (Д5) `FindProjectUserQuery`: явный `where('project_id', ...)` из `ProjectContext` в дополнение к глобальному скоупу (защита в глубину); проверить: guard 0.11(e) и тесты users зелёные без правок.
  - Сделано: явный `where('project_id', $context->required())` в query; guard 0.11(e) зелёный без правок.
- [x] 1.5 (Д10) `content-worker` в `infra/compose/compose.yaml`: `--queue=default,media`; проверить `docker compose config`; в отметке зафиксировать напоминание о дренаже `queues:media` перед выкаткой.
  - Сделано: `--queue=default,media` у content-worker, `docker compose config` валиден. Перед выкаткой: проверить `LLEN queues:media` — накопленный бэклог обработается при старте воркера.

## 2. Pay

- [x] 2.1 (Д11-а) Нормализация валюты на записи: `UpsertPlanHandler` сохраняет `strtoupper($currency)` (валидация не ужесточается — 201 остаётся 201); миграция бэкфилла `UPPER(currency)` для `plans`, `payments`, `payment_transactions` (one-way, с комментарием почему down не восстанавливает регистр); проверить: план `usd` создаётся и отдаётся как `USD`.
  - Сделано: `strtoupper` в UpsertPlanHandler; one-way миграция бэкфилла `UPPER(currency)` по трём таблицам (verify проверил на живой sqlite: usd→USD, rub→RUB, eur→EUR).
  - Сделано: `strtoupper` в `UpsertPlanHandler`; миграция `0003_01_01_000001_uppercase_currency_backfill` (one-way, down пуст с обоснованием); бэкфилл проверен на живых данных (`rub` → `RUB`). Проверка «usd → USD» — в переписанном guard 7.5 (задача 2.2).
- [x] 2.2 (Д11-б) Вернуть строгую типизацию денег, снятую в 7.5: `Plan::price()`/`Payment::amount()` и Money на четырёх путях чтения (Subscribe/Renew/2 листенера); **обновить guard `guard: 7.5`**: было «план usd проходит цикл как есть, леджер хранит usd», стало «план usd сохраняется как USD, полный цикл включая возврат работает, леджер хранит USD». Проверить полным циклом платёжных тестов.
  - Сделано: `Plan::price()`/`Payment::amount()` возвращены, Money на всех четырёх путях чтения, комментарии «Б-список» сняты. Обновлён тест guard 7.5: было «план usd проходит цикл как есть, леджер хранит usd, возврат падал» — стало «usd сохраняется как USD, полный цикл включая возврат работает, леджер в USD (-5000 refund)». Мутационно: снятие strtoupper → красный.
  - Сделано: `Plan::price()`/`Payment::amount()` возвращены; Money в `SubscribeHandler`, `RenewSubscriptionHandler`, `RecordChargeInLedger`, `PushPaymentStatusEvent` (комментарии «Сырые поля… (Б-список)» удалены). Обновлён тест `guard: 7.5` (LowercaseCurrencyGuardTest): было «план usd проходит подписку/подтверждение как есть, леджер хранит usd, возврат падал 500», стало «план usd создаётся через admin API и сохраняется как USD, подписка/подтверждение/возврат работают, леджер и ответы отдают USD».
- [x] 2.3 (Д3+Д7) Оживить `provider_accounts`: порт `PaymentProvider` получает `configure(array $credentials): static` (no-op по умолчанию); `ProviderRegistry::for()` резолвит `ProviderAccount` проекта (acrossProjects; `'-'` и отсутствие записи → без конфига, поведение прежнее); убрать пометку «мёртвый код» из реестра. Проверить: тесты провайдеров зелёные; новый юнит-тест — адаптер получает credentials своего проекта.
  - Сделано: `configure(array): static` в порту (no-op в manual/null), Registry резолвит `ProviderAccount::acrossProjects()` и конфигурирует адаптер, `WITHOUT_PROJECT='-'` константой; `ProviderAccountTest` (3 кейса). Наблюдение на будущее: флаг `enabled` аккаунта реестром пока не учитывается — решить при подключении реальных провайдеров.
  - Сделано: `configure()` в порту и адаптерах (manual/null — no-op); `ProviderRegistry::for()` резолвит `ProviderAccount` и конфигурирует адаптер, `WITHOUT_PROJECT = '-'` вынесен в публичную константу (гейтвей вебхука на неё переведён, докблоки про дефект Б7/9.2 заменены описанием живого механизма). Добавлен тест `ProviderAccountTest` (3 кейса: credentials своего проекта; '-'/проект без аккаунта — без конфига; пустые credentials).
- [x] 2.4 (Д4) 7.9-A: миграция — nullable `project_id` в `payment_webhook_events` + бэкфилл из `payments` по `payload->payment_id`; `RegisterWebhookHandler` заполняет `project_id` сразу, если платёж резолвится; `ProcessWebhookEventJob` проставляет при обработке. 7.9-B: `BelongsToProject` НЕ вешается (guard 0.8 доказал поломку приёма) — инвариант выполняется колонкой; **обновить guard 0.8**: было «событие пишется без project_id», стало «приём без контекста работает, после обработки project_id равен проекту платежа, у нерезолвируемого — NULL». Проверить: webhook-тесты + накат/откат миграции.
  - Сделано: миграция nullable `project_id` + бэкфилл из payments (накат/откат проверены); `RegisterWebhookHandler` пишет project_id сразу при резолве, `ProcessWebhookEventJob` — при обработке; `BelongsToProject` не повешен (обоснование в докблоке модели). Обновлён тест guard 0.8: было «событие пишется без project_id» — стало «приём без контекста работает, project_id = проект платежа, у нерезолвируемого NULL + статус failed»; добавлен 4-й кейс. Снимки webhook-* не тронуты.
  - Сделано: миграция `0003_01_01_000002_add_project_id_to_payment_webhook_events` (бэкфилл PHP-циклом — json-extraction различается между sqlite/pgsql; накат/откат и бэкфилл проверены на живых данных); `RegisterWebhookHandler` резолвит платёж acrossProjects и пишет `project_id` сразу; `ProcessWebhookEventJob` проставляет при обработке; обоснование отсутствия скоупа — в докблоке `WebhookEvent`. Обновлён тест `guard: 0.8` (WebhookProjectContextContractTest): было «событие пишется без project_id (колонки нет)», стало «приём без контекста работает, project_id заполнен сразу при резолвимом платеже и равен проекту платежа после обработки, у нерезолвируемого — NULL и статус failed» (добавлен кейс нерезолвируемого платежа). Снимки ответов вебхука не изменились.
- [x] 2.5 (Д2) `RefundPaymentHandler`: `DB::transaction(..., attempts: 3)`; при исключении после успешного возврата у провайдера — критический лог (payment_id, provider, amount_minor, currency) через `LoggerInterface`. Проверить: юнит-тест с падающей транзакцией — лог записан, исключение проброшено; существующие refund-тесты зелёные.
  - Сделано: `DB::transaction(..., 3)` + critical-лог `pay.refund.persist_failed` с полным контекстом + rethrow; `RefundRecoveryTest`. Verify доказал end-to-end: реальное падение транзакции пишет CRITICAL в laravel.log, исключение проброшено, refunded_minor откатился; понижение уровня лога — красный тест.
  - Сделано: `DB::transaction(..., 3)` + try/catch с `logger->critical('pay.refund.persist_failed', ...)` и rethrow; `LoggerInterface` конструктором; докблок про «известный дефект» заменён на описание восстановимости. Добавлен тест `RefundRecoveryTest` (падение на диспатче события: лог записан с полным контекстом, исключение проброшено, транзакция откатилась).

## 3. Auth и Analytics

- [x] 3.1 (Д9) Доставка reset-токена сайта: `PasswordResetTokens::issue()` возвращает plain-токен; `SitePasswordResetNotification` (mail) в `auth/src/Infrastructure/Notifications/`; `User` += `Notifiable`; `ForgotSitePasswordHandler` шлёт уведомление. Admin-ветка не меняется. Проверить: `Notification::fake()` — уведомление уходит существующему email и не уходит неизвестному; снимок ответа `{"sent": true}` не изменился.
  - Сделано: `issue()` возвращает plain (в БД — только hash), `SitePasswordResetNotification` (mail), `User` += Notifiable, handler шлёт уведомление; admin-ветка не тронута. Тест завершает ПОЛНЫЙ цикл: plain из notification → reset-password 200 → login новым паролем; неизвестный email → assertNothingSent; ответ `{"sent": true}` и его снимок не изменились.
  - Сделано: `issue()` возвращает plain (в БД по-прежнему только sha256-хэш); `ForgotSitePasswordHandler` берёт модель пользователя и шлёт `SitePasswordResetNotification`; admin-ветка не использует возвращаемое значение (комментарий «вне MVP» сохранён у вызова). Новые тесты в SiteAuthTest: доставленный токен проходит полный цикл reset-password + login новым паролем; неизвестный email — `assertNothingSent`. Снимки site-forgot-password* не тронуты; pest auth 281 зелёный.
- [x] 3.2 (Д12) Экспорт/отчёты: `ReportPeriodRequest::validationData()` → `$this->query()` (тело игнорируется целиком); **обновить guard 0.9(a)**: было «невалидное тело → 422», стало «тело игнорируется: даты в теле → 202 с дефолтным окном»; **обновить снимок** 422-на-тело (analytics-export-422-*) на новое поведение либо удалить с записью. Проверить: невалидный query по-прежнему 422 (снимки query-веток не тронуты).
  - Сделано: `validationData()` → `$this->query()` — тело игнорируется целиком. Обновлён тест guard 0.9(a): было «невалидное тело → 422» — стало «даты в теле игнорируются → 202 с дефолтным окном [now-30d; now]»; снимок 422-на-тело переадресован query-источнику без изменения байтов; 422 на невалидный query и все query-снимки — нетронуты.
  - Сделано: `validationData()` = `query()`. Обновлён guard 0.9(a) «export validates the json body it never reads»: было «невалидное тело → 422, джоба не ставится», стало «export ignores the json body entirely: невалидное тело → 202, джоба с дефолтным окном». Обновлён тест «contract: analytics export invalid period»: было — невалидный период в JSON-теле → снимок analytics-export-422, стало — тот же невалидный период в query («…invalid period in query») → тот же снимок analytics-export-422 (байт-в-байт, 422-контракт export переадресован каноничному источнику; тело больше нигде не даёт 422). Guard-тесты (b)(c)(d) и снимки query-веток без правок; pest analytics 145 зелёный.

## 4. Финализация

- [x] 4.1 Полный гейт: pest всех четырёх сервисов (строгий снапшот-режим), pint, stan, `./tools/refactor-inventory.sh --strict` = 0, архитектурный гейт зелёный.
  - Сделано: lint 4/4, stan 4/4, pest в строгом снапшот-режиме — auth 281, content 236, analytics 145, pay 171 (833 теста, все зелёные), инвентарь --strict = 0, архитектурный гейт зелёный.
- [x] 4.2 Сверка ущерба: `git status` по `**/__snapshots__/**` — изменены ТОЛЬКО снимки, заявленные в задачах 2.2, 2.4, 3.2; каждый изменённый файл соотнесён с задачей. Обновить README реестра в proposal.md: все 12 пунктов закрыты или переадресованы.
  - Сделано: `git status` по `__snapshots__` — **0** изменённых/удалённых файлов (обновления guard-тестов затронули только код тестов, тела ответов не менялись — лучше заявленного); реестр в proposal.md обновлён: 12/12 закрыты.
- [x] 4.3 Прогон на стенде: `./tools/cms up`, `tools/smoke.sh` — SMOKE OK; `LLEN queues:media` проверен до включения воркера.
  - Сделано: `LLEN queues:media` = 0 до включения воркера; стек перезапущен, health ok, `tools/smoke.sh` — SMOKE OK.
