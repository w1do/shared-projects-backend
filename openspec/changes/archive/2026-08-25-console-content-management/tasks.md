# Tasks: console-content-management

> Сценарии Playwright опираются на harness из `admin-e2e-playwright`: запуск — `./tools/cms e2e [--headed]`.
> После правок в `frontends/admin` панель в контейнере нужно перезапустить, иначе сценарии проверят прежнюю сборку.

## 1. Дерево в слое данных

- [x] 1.1 Выяснить фактическое поведение `DELETE /content/categories/{id}` для категории с потомками и с привязанными постами (запросом к платформе на тестовом поддереве); зафиксировать результат в `design.md` — от него зависит текст предупреждения в задаче 3.4
- [x] 1.2 Добавить в модель категории слоя данных родителя и уровень вложенности, приведя дерево платформы (`children`) к плоскому списку в префиксном порядке; проверить юнит-тестом на фикстуре из трёх уровней: порядок обхода и `depth` соответствуют дереву
- [x] 1.3 Вывести операцию перемещения в сервисный слой поверх `POST /content/categories/{id}/move` и прекратить безусловную отправку `parent_id` в `updateCategory` (она выбрасывала узел в корень при переименовании); проверить на живом API, что переименование не меняет родителя
- [x] 1.4 Реализовать вычисление списка недопустимых родителей (сам узел и его потомки) из загруженного дерева; проверить юнит-тестом: для узла с потомками список содержит его самого и всё поддерево

## 2. Демо-данные

- [x] 2.1 Углубить `DemoContentSeeder` до трёх уровней и убрать небезопасный выбор проекта (только явный `DEMO_PROJECT_ID`, команда `./tools/cms seed-demo` берёт его из auth-service); проверить повторным прогоном — 11 узлов, третий уровень на месте, дубликатов нет

## 3. Экран категорий

- [x] 3.0 Перевести `categories/add`, `categories/[id]/edit` и `blogs/[slug]/edit` на клиентскую загрузку (как уже сделано для списка): серверный рендер не знает текущего проекта — он в cookie браузера, и страницы падали с `project_missing`; проверить, что все три открываются в режиме `api`

- [x] 3.1 Показать вложенность в списке категорий (отступ по уровню, родитель различим); зеркалить правку в `frontends/source-admin` и проверить `diff -r` по `src/components` — расхождений нет
- [x] 3.2 Добавить выбор родителя в форму создания и редактирования категории, исключив недопустимых родителей из выбора; проверить в браузере, что для узла с потомками они в списке не предлагаются
- [x] 3.3 Добавить действие перемещения категории к другому родителю; проверить, что потомки следуют за узлом и что перемещение в корень работает
- [x] 3.4 Привести диалог удаления в соответствие с фактическим поведением платформы из задачи 1.1: предупреждение о судьбе потомков и привязанных постов до подтверждения; проверить, что отмена не меняет дерево
- [x] 3.5 Показать понятную ошибку при отказе платформы на операции с категорией, сохранив работоспособность раздела; проверить на заведомо недопустимом перемещении

## 4. Экран блогов

- [x] 4.1 Довести список постов: статус, дата публикации, привязанные категории, отбор по статусу; проверить в браузере на демо-данных (одна публикация, один черновик)
- [x] 4.2 Довести создание и редактирование поста, включая привязку к категориям с выбором из дерева проекта; проверить, что изменения видны после повторного открытия поста
- [x] 4.3 Реализовать смену статуса по текущему состоянию поста, без собственной логики допустимых переходов; проверить публикацию черновика и обработку отказа платформы — статус не меняется
- [x] 4.4 Довести просмотр и восстановление ревизий; проверить, что после восстановления содержимое соответствует выбранной версии
- [x] 4.5 Зеркалить все правки экрана блогов в `frontends/source-admin`; проверить `diff -r` по `src/components` и `src/app` — расхождений нет

## 5. Сценарии Playwright

- [x] 5.1 Реализовать в harness подготовку и уборку тестовых данных: категории удаляются в `finally` (удаление снимает всё поддерево), пост переводится в архив — удалить его платформа не умеет; проверить двумя прогонами подряд — активных тестовых данных не накапливается
- [x] 5.2 Сценарий дерева: создание подкатегории и корневой категории, вложенность видна в интерфейсе; проверить прогоном
- [x] 5.3 Сценарий перемещения: узел с потомками переносится к другому родителю и в корень, потомки следуют за ним; проверить прогоном
- [x] 5.4 Сценарий запрета цикла: собственные потомки не предлагаются как родитель, а попытка недопустимого перемещения отклоняется и не меняет дерево; проверить прогоном
- [x] 5.5 Сценарий жизненного цикла поста: создание → привязка к категории → публикация → правка → восстановление ревизии; проверить прогоном
- [x] 5.6 Прогнать весь набор `./tools/cms e2e` дважды подряд и один раз `--headed`; убедиться, что все сценарии зелёные и состояние проекта не изменилось

## 6. Документация

- [x] 6.1 Обновить `docs/admin-console.md`: дерево категорий (вложенность, перемещение, поведение удаления), возможности раздела блогов, перечень сценариев; проверить, что описанное соответствует поведению панели
