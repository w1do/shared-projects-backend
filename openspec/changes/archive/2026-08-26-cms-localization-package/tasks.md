# Tasks: cms-localization-package

> Зависимость: реализованный `cms-ai-package` (порт `AiOperations::translate`). Начинать группу 4 до него нельзя.
> Скиллы перед работой: `.ai/skills/spatie-laravel-php`, `.ai/skills/sluggable-development`, `.ai/skills/laravel-permission-development`.

## 1. Словарь переводов

- [x] 1.1 Создать пакет `packages/cms/localization` (каркас по канону, path-репозиторий в content-service) с миграцией `translations` (`project_id`, `key` уникальный на проект, `values` jsonb, `machine` jsonb); проверить миграцию накатом и откатом
- [x] 1.2 Реализовать модель Translation (`BelongsToProject`) и CRUD: DTO/Handlers/контроллер `Api/V1` по конвейеру FormRequest → DTO → Handler → Resource; валидация локалей против локалей проекта (неизвестная — 422); проверить Pest-тестами создание/чтение/изменение/удаление и отказ по неизвестной локали
- [x] 1.3 Добавить права `content.translations.view|manage` в `ContentManifest`, закрыть ими маршруты; проверить тестом, что оператор без права получает 403
- [x] 1.4 Реализовать отдачу словаря `GET /content/translations?locale=…` — плоский `{key: value}` с откатом на локаль по умолчанию; проверить тестом отдачу и откат

## 2. Версия переводов в bootstrap

- [x] 2.1 Ввести счётчик версии переводов в content-service (инкремент при любой записи словаря) и уведомление auth-сервиса по существующему cache-bust каналу с `BootstrapCache::bump()`; проверить тестом content-service рост версии
- [x] 2.2 Отдавать реальную версию в `bootstrap.translations_version` вместо заглушки; проверить тестом auth-service, что после уведомления версия в bootstrap меняется

## 3. Переводимое имя категории

- [x] 3.1 Подключить `spatie/laravel-translatable` в content-service, перевести `Category.name` на `HasTranslations`; миграция string→jsonb с переносом существующих значений в локаль по умолчанию и рабочим откатом; проверить тестом чтение прежних имён после миграции
- [x] 3.2 Совместимость API: `UpsertCategoryDTO.name` принимает строку и набор по локалям; `CategoryDTO` отдаёт `name` (строка запрошенной/дефолтной локали) и `name_translations`; slug остаётся единым из локали по умолчанию; проверить тестами обе формы записи и чтение отсутствующей локали
- [x] 3.3 Проверить неизменность поведения дерева после миграции: существующие Pest-тесты категорий и e2e-набор `./tools/cms e2e` (дерево, DnD, посты) — зелёные без правок
- [x] 3.4 Обновить `DemoContentSeeder` на переводимые имена (en+ru) и прогнать `./tools/cms seed-demo` — идемпотентность сохранена

## 4. Автоперевод (после cms-ai-package)

- [x] 4.1 Реализовать `TranslateMissing` (Command/Handler/Job по канону Jobs) для словаря и категорий поверх `AiOperations::translate`: переводится только недостающее, существующее не перезаписывается, уже полные записи не вызывают провайдера; проверить тестами с фейком порта
- [x] 4.2 Пометка `machine` у автопереводов и снятие её при ручном изменении значения; признак в API словаря; проверить тестами
- [x] 4.3 Отказоустойчивость: ошибка AI не меняет записи, повторный запуск дозаполняет только оставшееся; проверить тестами на фейке с падением посреди пачки
- [x] 4.4 Эндпоинт запуска автоперевода (`POST /content/translations/translate-missing`, право `content.translations.manage`) с постановкой Job; проверить тестом постановку и 403 без права

## 5. Панель

- [x] 5.1 Слой данных панели: чтение `name`/`name_translations` категории без изменения экранов (совместимость подтверждается текущими e2e); проверить, что панель работает против обновлённого API без правок компонентов
- [x] 5.2 Живой прогон: словарь из пары ключей через API, автоперевод недостающих `ru`-значений через Polza (ключ из окружения), проверка `translations_version` в bootstrap до и после

## 6. Документация

- [x] 6.1 Обновить `docs/`: словарь (форма key → {locale: value}), переводимые поля, автоперевод и пометка machine, ENV; проверить соответствие поведению
