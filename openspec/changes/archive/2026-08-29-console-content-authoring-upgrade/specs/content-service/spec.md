# content-service

## MODIFIED Requirements

### Requirement: Посты

Admin API SHALL поддерживать CRUD постов проекта: заголовок, slug, контент, локаль, автор, привязка к категориям, обложка и баннер — ссылки на медиа-файлы того же проекта, статусы `draft → scheduled → published → archived` (недопустимые переходы отклоняются), отложенная публикация по времени. Обложка и баннер MAY быть пустыми и MUST ссылаться только на медиа текущего проекта; ссылка на медиа другого проекта или на несуществующий файл SHALL отклоняться. Admin и публичное API SHALL отдавать адреса изображений поста ссылками платформы. Каждое сохранение SHALL создавать ревизию с возможностью восстановления.

#### Scenario: Отложенная публикация

- WHEN пост в статусе scheduled со временем в будущем
- THEN после наступления времени он публикуется автоматически и появляется в публичном API

#### Scenario: Восстановление из ревизии

- WHEN оператор восстанавливает пост из ревизии
- THEN контент равен снимку, создаётся новая ревизия

#### Scenario: Изображения поста в ответе

- WHEN клиент запрашивает пост с заданной обложкой
- THEN ответ содержит ссылку платформы на изображение обложки

#### Scenario: Чужое медиа

- WHEN пост сохраняется со ссылкой на медиа другого проекта
- THEN запрос отклоняется ошибкой валидации, пост не изменяется

### Requirement: Категории на nested sets

Категории проекта SHALL храниться деревом неограниченной вложенности (nested sets). Admin API SHALL поддерживать создание, переименование, удаление и **перемещение узла вместе со всем его поддеревом** в любое место дерева; целостность дерева сохраняется при конкурентных операциях. Admin API SHALL поддерживать удаление набора категорий одним запросом и удаление всех категорий проекта одним запросом; обе операции MUST выполняться целиком или не выполняться вовсе и MUST NOT оставлять узлы без родителя. Посты MUST сохраняться при удалении категорий и терять только привязки к удалённым узлам. Посты привязываются к категориям; публичное API отдаёт дерево и выборки по категории с потомками.

#### Scenario: Перемещение поддерева

- WHEN оператор перемещает категорию с потомками под другого родителя
- THEN всё поддерево сохраняет внутреннюю структуру, порядок и связи постов, выборки по новому родителю включают перемещённые узлы

#### Scenario: Выборка с потомками

- WHEN публичный запрос просит посты категории с учётом вложенности
- THEN возвращаются посты самой категории и всех её потомков

#### Scenario: Удаление набора категорий

- WHEN оператор удаляет несколько категорий одним запросом
- THEN исчезают они и их поддеревья, оставшееся дерево согласовано, посты сохранены

#### Scenario: Очистка каталога

- WHEN оператор удаляет все категории проекта одним запросом
- THEN у проекта не остаётся категорий, посты сохранены и не имеют привязок к категориям

#### Scenario: Массовое удаление не выполняется частично

- WHEN массовое удаление прерывается ошибкой
- THEN ни одна из категорий запроса не удалена

### Requirement: Медиа

Admin API SHALL позволять загружать медиа (mime, размер, alt) и использовать их в постах/страницах/SEO (og:image); превью-варианты SHALL генерироваться асинхронно, не в HTTP-запросе. Admin API SHALL позволять импортировать изображение по внешней ссылке: файл скачивается в хранилище проекта и далее ничем не отличается от загруженного вручную. Импорт SHALL проверять тип и размер файла по тем же правилам, что и загрузка, и SHALL ограничивать время скачивания; неудачный импорт MUST NOT создавать запись медиа.

#### Scenario: Загрузка изображения

- WHEN оператор загружает изображение
- THEN ответ приходит сразу, варианты появляются позже

#### Scenario: Импорт по ссылке

- WHEN оператор импортирует изображение по внешней ссылке
- THEN файл появляется в медиа проекта и отдаётся по ссылке платформы

#### Scenario: Недопустимый файл по ссылке

- WHEN по ссылке отдаётся файл неподдерживаемого типа или превышающий предел размера
- THEN импорт отклоняется, запись медиа не создаётся
