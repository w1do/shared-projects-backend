## 1. Категории: массовые операции в content-service

- [x] 1.1 Добавить `Application/Commands/DeleteCategoriesCommand.php` (список id) и `Application/Handlers/DeleteCategoriesHandler.php`: удаление набора узлов одной транзакцией, чужие и несуществующие id пропускаются; проверить Pest-тестом, что уходят выбранные узлы с поддеревьями, а посты остаются
- [x] 1.2 Добавить `Application/Commands/PurgeCategoriesCommand.php` и `Application/Handlers/PurgeCategoriesHandler.php`: удаление всех категорий проекта одним запросом; проверить Pest-тестом, что каталог пуст, посты сохранены и лишились привязок
- [x] 1.3 Добавить `Presentation/Http/Api/V1/Requests/Category/BulkDeleteCategoriesRequest.php` (обязательный непустой `ids`, целые) и действия `bulkDestroy`/`purge` в `CategoryController` с OpenAPI-аннотациями; проверить, что пустой `ids` даёт 422
- [x] 1.4 Зарегистрировать `POST content/categories/bulk-delete` и `DELETE content/categories` в `packages/cms/content/routes/admin.php` под `content.categories.manage`; проверить Pest-тестом отказ по правам и 204 при успехе
- [x] 1.5 Убедиться, что массовое удаление не выполняется частично: Pest-тест с ошибкой внутри транзакции оставляет дерево нетронутым

## 2. Посты: изображения в content-service

- [x] 2.1 Миграция в `packages/cms/content/database/migrations`: `cover_media_id` и `banner_media_id` у постов, nullable, FK на `media_files` с `nullOnDelete`; проверить `./tools/cms migrate content` и откат миграции
- [x] 2.2 Добавить поля в `Post`, `UpsertPostDTO` (`int|Optional|null`) и `UpsertPostHandler`; проверить Pest-тестом, что непереданное поле не обнуляет прежнее изображение, а переданный `null` — обнуляет
- [x] 2.3 Валидация в `UpsertPostRequest`: медиа существует и принадлежит текущему проекту; проверить Pest-тестом 422 на медиа чужого проекта
- [x] 2.4 Отдавать обложку и баннер вложенным объектом медиа (`id`, `url`, `alt`) в `PostDTO`/`PostResource` и в публичном API контента; проверить снимками ответов admin- и публичного поста
- [x] 2.5 Проверить Pest-тестом, что удаление медиа-файла оставляет пост без изображения и не удаляет сам пост

## 3. Медиа: импорт по внешней ссылке

- [x] 3.1 Добавить порт `Domain/Contracts/RemoteFileFetcher.php` и реализацию в `Infrastructure/Persistence` (или `Infrastructure/Http`) с таймаутом, пределом размера, ограничением редиректов и запретом приватных/loopback-адресов; проверить unit-тестами на подменённом клиенте
- [x] 3.2 Добавить `Application/Commands/ImportMediaCommand.php`, `Application/Handlers/ImportMediaHandler.php` и DTO операции: скачанный файл проходит те же проверки mime/размера, что и загрузка, и уходит в `GenerateMediaVariantsJob`; проверить Pest-тестом создание `MediaFile` и постановку джобы
- [x] 3.3 Добавить `Requests/Media/ImportMediaRequest.php` (обязательный `url` со схемой http/https, необязательный `alt`), действие `import` в `MediaController` с OpenAPI-аннотацией и маршрут `POST content/media/import` под `content.media.manage`; проверить отказ по правам и 201 при успехе
- [x] 3.4 Проверить Pest-тестом, что недоступная ссылка и файл недопустимого типа отклоняются без создания записи медиа

## 4. Поиск изображений в research-пакете

- [x] 4.1 Добавить `Domain/ValueObjects/ImageResultItem.php` (ссылка, превью, ширина, высота, источник) и расширить порт `Domain/Contracts/SerpSearchClient.php` операцией `searchImages()`; проверить, что архитектурный гейт проходит
- [x] 4.2 Реализовать `searchImages()` в `Infrastructure/Search/SerpApiSearchClient.php` (разбор ответа поиска картинок, предел числа результатов) и добавить движки поиска картинок в `Domain/Enums`; проверить unit-тестом на подменённом HTTP-клиенте, включая пустую выдачу и отказ службы
- [x] 4.3 Добавить настройки движка и предела результатов в `packages/cms/research/config/cms-research.php` (значения из окружения); проверить, что при незаданном ключе поднимается `ResearchConfigurationException`
- [x] 4.4 Добавить `Application/Queries/SearchImagesQuery.php`, `Presentation/.../Requests/SearchImagesRequest.php`, `Resources/ImageResultResource.php` и контроллер поиска изображений; маршрут `GET content/images/search` под `content.media.manage`; проверить Pest-тестом ответ, отказ по правам и понятную ошибку при недоступной службе

## 5. Пресеты схем инструкций

- [x] 5.1 Добавить `Infrastructure/Persistence/SchemaPresetCatalog.php`: пресеты категорий, поста и SEO с полями, типами, обязательностью, описанием и применимыми категориями инструкций; проверить unit-тестом состав каталога
- [x] 5.2 Добавить `Application/Queries/ListSchemaPresetsQuery.php`, `Resources/SchemaPresetResource.php`, действие в `InstructController` и маршрут `GET content/instructs/schema-presets`; проверить снимком ответа
- [x] 5.3 Добавить Pest-тест соответствия: состав SEO-пресета совпадает с полями `UpsertSeoDTO`, пресет категорий — с формой, которую разбирает `CreateCategoryTreeAction`; проверить, что тест падает при добавлении поля только в одну сторону

## 6. Создание проекта в auth-service

- [x] 6.1 Сделать `key` необязательным в `CreateProjectDTO` (`string|Optional`) и `CreateProjectRequest`; проверить Pest-тестом, что запрос с одним `name` проходит валидацию
- [x] 6.2 Вывести ключ из названия в `CreateProjectHandler` (транслитерация, kebab-case, предел длины, числовой суффикс при занятости, подбор в той же транзакции); проверить Pest-тестами создание по названию, кириллическое название и два проекта с одинаковым названием
- [x] 6.3 Проверить Pest-тестом, что созданный по названию проект пуст, создатель получает роль `owner`, сервисы по умолчанию включены, а явно переданный занятый `key` по-прежнему даёт 422

## 7. Консоль: категории, посты и подбор изображения

- [x] 7.1 Добавить выбор строк в `components/ui/data-display/tree-table.tsx` (`checkbox`, `selectedRowIds`, `onSelectionChange`), пометку потомков выбранного родителя; зеркалировать файл в `frontends/source-admin` и проверить `diff -rq admin/src/components source-admin/src/components`
- [x] 7.2 Добавить в `lib/admin/data-source/category-tree.ts` построение полного пути узла по slug'ам предков; покрыть node-тестом, включая одноимённые категории в разных ветках
- [x] 7.3 Провести выбор через `useCategoriesPanel`/`CategoriesPanel` в древовидном режиме и показать полные пути в `category-delete-dialog`; проверить в браузере, что счётчик выбранного и пути соответствуют дереву
- [x] 7.4 Добавить действие «удалить все категории» в тулбар категорий с отдельным подтверждением и числом удаляемых, недоступное на пустом каталоге; проверить в браузере оба сценария
- [x] 7.5 Перевести массовое удаление и очистку в `lib/admin/data-source/platform/content.ts` и `hooks/admin/categories` на новые эндпоинты (один запрос вместо серии); проверить в браузере обновление дерева и сообщение об ошибке при отказе платформы
- [x] 7.6 Перевести загрузку изображений поста на медиа-API проекта: загрузка файла из формы поста уходит в `POST content/media`, в форме хранится ссылка платформы; проверить в браузере, что изображение переживает перезагрузку страницы
- [x] 7.7 Провести обложку и баннер через `blog-form-schema`, маппер и мутации постов; показать обложку в списке постов с единой заглушкой при её отсутствии; проверить node-тестами маппера и в браузере
- [x] 7.8 Добавить диалог подбора изображения (запрос, сетка результатов с превью и источником, выбор → импорт → подстановка) и кнопку в блоке изображений поста, видимую по праву `content.media.manage`; предзаполнять запрос заголовком поста; проверить в браузере выбор, отмену и отсутствие кнопки без права

## 8. Консоль: ресёрч, инструкции, создание проекта

- [x] 8.1 Пересобрать раздел ресёрча на компонентах вёрстки (`PageHeader`, `Card variant="form-section"`, штатный список, `Badge`, `Dialog`, `Skeleton`) со структурой `sections/<name>/index.tsx`; проверить в браузере, что раздел не отличается от блогов по шапке, отступам и элементам
- [x] 8.2 Привести состояния раздела ресёрча к общим: скелетон формы списка, единое пустое состояние с действием запуска, отбор по состоянию штатным элементом, выполнение действия на кнопке; проверить в браузере все три состояния
- [x] 8.3 Пересобрать раздел инструкций на тех же компонентах (список с бейджами категории, происхождения и публикации, штатные поля и переключатель, диалог удаления); проверить в браузере
- [x] 8.4 Добавить `lib/admin/instructs/schema-fields.ts`: преобразование «поля ↔ JSON Schema» в обе стороны, включая вложенные объекты и массивы объектов, и признак «схема сложнее редактора»; покрыть node-тестами, в том числе круговым преобразованием
- [x] 8.5 Собрать визуальный редактор схемы в форме инструкции (добавление и удаление полей, тип, обязательность, описание) с переключением в JSON-режим без потери полей; схему с `oneOf`/`$ref` открывать сразу в JSON-режиме; проверить в браузере оба пути
- [x] 8.6 Подключить пресеты: выбор пресета, предложение по категории инструкции по умолчанию, подтверждение замены заполненной схемы; проверить в браузере применение пресета SEO и правку его полей
- [x] 8.7 Добавить кнопку «Новый проект» в `AdminTopbar` рядом с индикатором состояния сайта, видимую по `is_super_admin`, и диалог с одним полем названия; проверить в браузере видимость под супер-админом и её отсутствие под обычным оператором
- [x] 8.8 Провести создание проекта через слой данных: запрос создания, установка `project_key`, сброс кэша запросов и перечитывание bootstrap; проверить в браузере переход в новый пустой проект и возврат к прежнему
- [x] 8.9 Добавить все новые тексты в реестр `console.*` (`lib/admin/console-texts.ts`); проверить, что в разделах нет строк мимо реестра

## 9. Проверки и сборка

- [x] 9.1 Прогнать `composer lint` и `composer stan` в затронутых сервисах; убедиться, что Pint и Larastan level 8 проходят
- [x] 9.2 Прогнать `./tools/cms test content` и `./tools/cms test auth`; убедиться, что архитектурный гейт `ArchitectureGateTest` и все новые тесты зелёные
- [x] 9.3 Прогнать `./tools/refactor-inventory.sh --strict`; убедиться, что новые классы лежат по канону слоёв
- [x] 9.4 Прогнать node-тесты консоли и `diff -rq frontends/admin/src/components frontends/source-admin/src/components`; убедиться, что вывод diff пуст
- [x] 9.5 Пересобрать единый swagger (`./tools/cms api`); убедиться, что новые маршруты попали в спецификацию
