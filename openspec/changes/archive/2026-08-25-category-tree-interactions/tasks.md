# Tasks: category-tree-interactions

> Зависимости: реализованный `console-content-management`, harness `admin-e2e-playwright`.
> Новые npm-зависимости панели запрещены (`bun.lock` пересчитать нечем) — DnD только на нативных событиях.
> После правок панели: `docker restart platform-admin-front-1`, иначе сценарии проверяют прежнюю сборку.

## 1. Бекенд: порядок соседей

- [x] 1.1 Дореализовать `position` в `MoveCategoryHandler` (вставка среди соседей через nested set; без `position` — в конец, как сейчас); проверить Pest-тестом: перемещение в позицию 0 и в середину даёт ожидаемый порядок, вызов без `position` ведёт себя по-прежнему
- [x] 1.2 Прогнать полный набор тестов content-service через `./tools/cms test content-service` — регрессий нет

## 2. TreeTable (дизайн-система)

- [x] 2.1 Реализовать `ui/data-display/tree-table.tsx` на примитивах `table.tsx`: плоские узлы с `depth`, колонки, сворачивание ветвей (свёрнутый узел остаётся целью вложения); проверить рендер трёхуровневого дерева и сворачивание в браузере
- [x] 2.2 Реализовать DnD: три зоны броска (перед/внутрь/после) с различимой подсветкой, запрещённые цели (предикат) не принимают бросок; колбэк `onMove(nodeId, parentId, position)`; проверить в браузере все три зоны и запрет на собственное поддерево
- [x] 2.3 Пометка «в полёте» на время запроса и возврат прежнего вида при ошибке; проверить подменой ответа `move` на 422 — тост с текстом платформы, дерево прежнее

## 3. CategoryTreeSelect (дизайн-система)

- [x] 3.1 Реализовать `ui/inputs/category-tree-select.tsx` (Popover + Command): дерево с отступами, поиск с показом предков найденного узла, «ничего не найдено», опция корня; проверить в браузере поиск вложенного узла
- [x] 3.2 Режимы single и multiple (чекбоксы, выбранное видно в триггере) и `disabled`-узлы по переданному Set; проверить, что недопустимые узлы не выбираются

## 4. Применение на экранах

- [x] 4.1 Перевести экран категорий (режим api) на `TreeTable`: снять пагинацию, отключать DnD при активном поиске; mock-режим оставить на прежней таблице; проверить в браузере оба режима
- [x] 4.2 Подключить перемещение перетаскиванием к `moveCategory` с перечитыванием дерева; проверить в браузере вложение, порядок и перенос в корень — состояние переживает перезагрузку
- [x] 4.3 Перевести выбор родителя (формы категории, диалог «Move to…») на `CategoryTreeSelect` single с недопустимыми узлами; убрать `parent-options.ts`; проверить, что прежние сценарии перемещения формой работают
- [x] 4.4 Перевести привязку категорий в форме поста на `CategoryTreeSelect` multiple вместо блока чекбоксов; проверить сохранение и повторное открытие поста — привязки те же
- [x] 4.5 Зеркалить все правки в `frontends/source-admin`; `diff -r` по `src/components` и `src/app` — расхождений нет

## 5. Сценарии Playwright

- [x] 5.1 Обновить сценарии `content-management.spec.ts` под новые компоненты (выбор родителя и категорий поста через новый селект); весь прежний набор зелёный
- [x] 5.2 Сценарий DnD-вложения: перетащить узел с потомком на другую строку — стал потомком, поддерево при нём; проверить прогоном
- [x] 5.3 Сценарий DnD-порядка: перетащить узел между соседями — позиция сохранена после перезагрузки; проверить прогоном
- [x] 5.4 Сценарий запрета: бросок на собственного потомка не меняет дерево, цель показана запрещённой; проверить прогоном
- [x] 5.5 Сценарий селекта: поиск вложенной категории, выбор в multiple-режиме, недоступность своих потомков в single-режиме; проверить прогоном
- [x] 5.6 Полный набор `./tools/cms e2e` дважды подряд и один раз `--headed` — зелёный, состояние проекта не меняется

## 6. Документация

- [x] 6.1 Обновить `docs/admin-console.md`: перетаскивание (зоны, запреты, альтернатива без мыши), селект категорий, судьба пагинации; проверить соответствие описанного поведению
