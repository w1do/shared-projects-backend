# Tasks: console-service-sync

## 1. Карта разделов и снимок доступа

- [x] 1.1 Добавить в `src/lib/admin/sidebar-config.ts` ключ раздела каждому пункту меню и быстрому действию (порядок, подписи и иконки не менять); проверить `diff` по подписям и порядку — изменились только добавленные поля
- [x] 1.2 Завести карту требований «ключ раздела → `{service, permission}`» по таблице из `design.md`; разделы без сервиса помечены как демонстрационные; проверить юнит-тестом карты: шесть живых разделов имеют требование, остальные — нет
- [x] 1.3 Реализовать вычисление видимых ключей из ответа `bootstrap` (включённые `services[]` + `permissions[]`, `["*"]` = все); проверить на фикстурах: супер-администратор видит все живые разделы, оператор без права — не видит свой раздел, выключенный сервис убирает свои разделы
- [x] 1.4 Сохранять снимок при входе и при каждом успешном `bootstrap`: cookie `console_sections` + запись рядом с профилем оператора; проверить, что после входа cookie содержит ожидаемый список ключей

## 2. Меню и быстрые действия

> Визуальные сценарии (2.4, 3.2, 4.3) требуют harness Playwright — он вводится отдельным изменением и здесь считается предпосылкой.

- [x] 2.1 Реализовать селектор видимого меню: возвращает группы и пункты в исходном порядке, выбрасывает недоступные пункты и пустые группы; проверить юнит-тестом на снимке без прав на `blogs`
- [x] 2.2 Переключить `AdminSidebar.tsx` на результат селектора (единственная правка — источник списка) и зеркалить её в `frontends/source-admin`; проверить `diff -r` источника и панели по `src/components` — расхождений нет
- [x] 2.3 Отфильтровать `quickActions` тем же селектором; проверить, что действия скрытых разделов («Add product», «New promotion», «Import inventory», «Create collection», «Launch campaign») не отображаются
- [x] 2.4 Сценарий Playwright (headed) под супер-администратором: после входа в сайдбаре ровно Dashboard, Blogs, Categories, Customers, Team, Settings; групп без видимых пунктов нет; быстрые действия скрытых разделов отсутствуют; проверить утверждениями сценария и снимком экрана в артефактах прогона

## 3. Доступ к маршрутам

- [x] 3.1 Добавить в `src/proxy.ts` проверку скрытых разделов: сегмент после `/admin/` не входит в cookie `console_sections` → редирект на `/admin/unauthorized`; отсутствующая cookie навигацию не блокирует; проверить прямыми переходами
- [x] 3.2 Сценарий Playwright: прямой переход на `/admin/products` заканчивается на `/admin/unauthorized`, а `/admin/blogs` открывается штатно; убедиться, что состав маршрутов `app/admin` не изменился

## 4. Сохранность вёрстки и документация

- [x] 4.1 Убедиться, что страницы, компоненты, маршруты и демо-данные скрытых разделов остались в репозитории без изменений; проверить `git status` и CI-diff вёрстки
- [x] 4.2 Обновить `docs/admin-console.md` и README: правило видимости (сервис включён + есть право), таблица «раздел → сервис → право», перечень скрытых разделов и как вернуть раздел одной строкой карты
- [x] 4.3 Сквозной сценарий Playwright через gateway: вход → меню собрано по правам → скрытый раздел по прямому адресу даёт отказ → выключение сервиса `content` в настройках проекта убирает Blogs и Categories после повторного входа; прогон headed, чтобы шаги были видны на экране, с видео и трейсом в артефактах
