# Tasks: licensing-and-polymorphic-subscriptions

## 1. Фундамент shared/contracts

- [x] 1.1 `cms/shared`, namespace `Cms\Shared\Billing`: контракт `Subscribable` (`subscriptionPrice(): Money`, `subscriptionInterval(): DateInterval`, `subscriptionCode(): string`, `subscriptionName(): string`) и VO `Subscriber` (type + id, `subjectKey(projectId)`: `site_user` → `user:{p}:{id}` байт-в-байт, прочие → `{type}:{p}:{id}`) — VO обязан жить в shared, его порождает RequestIntrospection (1.3), а shared не зависит от pay; `cms/contracts`: события `Cms\Contracts\Events\SubscriptionStarted` и `SubscriptionPeriodExtended` (final readonly, скаляры: subscriptionId, projectId, subscriberType, subscriberId, subjectType, subjectId, periodEndsAt); unit-тесты `subjectKey` обоих форматов; `composer stan` обоих пакетов
- [x] 1.2 `Cms\Shared\Analytics`: обобщить вывод `project_id` из subject-ключа на шаблон `{type}:{project}:{id}` (прежний `user:{p}:{id}` — байт-в-байт как раньше); unit-тесты: старый формат, `organization:*`, ключ вне шаблона ведёт себя как сейчас
- [x] 1.3 `Cms\Shared\AuthClient\RequestIntrospection`: метод, отдающий `Subscriber('site_user', userId)` рядом с существующим `siteUserKey()` (строковый формат остаётся для аналитики); unit-тест деривации

## 2. Полиморфная модель подписок (cms/pay: схема и домен)

- [x] 2.1 Миграция `0003_01_02_*`: добавить `subscriptions.subscriber_type/subscriber_id` и `subject_type/subject_id`, бэкфилл ПОРТИРУЕМОЙ техникой (PHP-чанки через query builder или выражения, работающие в Postgres И sqlite — тестовый контур гоняет миграции на sqlite `:memory:`, `split_part` недопустим): `user_key` `user:{p}:{id}` → (`site_user`, `{id}`), `plan_id` → (`plan`, id); снять FK `plan_id`, дроп `user_key`/`plan_id`; индексы: единый составной (subscriber_type, subscriber_id, subject_type, subject_id) + отдельный (subject_type, subject_id); `payments`: rename `user_key` → `subject_key`; обратная миграция восстанавливает строку/FK; проверить накат+откат на sqlite (тесты) и Postgres-копии (создать подписку до миграции, сверить конверсию assertDatabaseHas)
- [x] 2.2 Домен pay: заменить `SiteUserKey` на `Cms\Shared\Billing\Subscriber` (из 1.1) по всему пакету; модель `Subscription` — fillable/PHPDoc, morphTo `subject()`, аксессор подписчика; модель `Payment` — fillable/PHPDoc `subject_key`; морф-алиас `plan` в PayServiceProvider; `Plan` реализует `Subscribable`; `SubscriptionPolicy::ownedBy/owns` по паре подписчика (семантика 404); фикс Д17: `past_due → past_due` разрешён; unit-тесты policy и перехода
- [x] 2.3 Конвейер pay: `SubscribeCommand` (Subscriber + subject-ссылка; сайтовый вход остаётся `plan_code`), `SubscribeHandler` (анти-дубль по 4 колонкам, subject через `Subscribable`, диспатч `SubscriptionStarted`, платёж с `subjectKey`), `RenewSubscriptionHandler` (цена/описание из `Subscribable`, subjectKey платежа из Subscriber), `ExtendSubscriptionPeriod` (интервал из subject, диспатч `SubscriptionPeriodExtended`; семантика сдвига guard 0.7 не меняется), `ChangeSubscriptionHandler` + `CreatePaymentCommand/Handler` (`subjectKey`), листенеры `PushPaymentStatusEvent`/`PushPaymentRefundEvent` (`$payment->subject_key`), `PlategaProvider` (`metadata.userId` ← `subject_key`), `PaymentAnalyticsProps` (props `plan`/`plan_id`/`plan_name` из subject — ключи прежние), queries (`FindSite*/ListSite*/List*` — ownedBy + морф eager-load); проверить существующими поведенческими тестами: SubscriptionRenewalTest (guard 0.7, идемпотентные ключи), PaymentAnalyticsTest (subject_key `user:proj-1:7` НЕ изменился), WebhookPlategaTest (CONFIRMED продлевает период), PlategaProviderTest (metadata.userId)

## 3. Полиморфные подписки: API и контракты

- [x] 3.1 Presentation pay: `SiteSubscriptionController` строит `Subscriber` из интроспекции (контракт `plan_code` + `X-User-Token` не меняется); `SubscriptionDTO/Resource` — вместо `user_key` объект `subscriber {type, id}`, предмет с типом/кодом; `PaymentDTO/Resource` — `subject_key`; фикстуры 9 тест-файлов (сырые `Model::create`, включая PlategaProviderTest/RefundRecoveryTest/PartialUpdateStateTest) переведены на новые колонки (завести `SubscriptionFactory`/`PaymentFactory`); проверить: SubscriptionTest, ListShapeMoneyGuardTest (mine без meta, деньги int), PublicContractTest 404-на-чужую — зелёные
- [x] 3.2 Переснять 19 снимков-контрактов с `user_key` (критерий полноты — `grep -rl user_key __snapshots__`) по одному, диффом подтверждая, что изменились только subscriber/subject_key/plan→subject-поля; текст-контракты доменных ошибок (`Subscription already exists.`) не меняются; `tools/smoke.sh` прогнать целиком — зелёный без правок jq-путей
- [x] 3.3 Admin-оформление подписки: `POST .../pay/subscriptions` (право `pay.subscriptions.manage`), FormRequest (subscriber_type/id, subject_type/id, опциональный `provider` → в `CreatePaymentDTO`; без него — провайдер из настроек проекта), резолв subject через морф + проверка `Subscribable`, локальный subscriber — существование в проекте; feature-тесты НА ЭТОМ ЭТАПЕ только с site_user+тарифным планом (403 без права, 422 на нерезолвящийся subject, оформление с provider=manual); кейсы организации/лицензионного плана — в 8.2 после создания licensing; RouteCoverageTest и swagger-аннотации обновлены

## 4. Каркас пакета licensing и механические гейты

- [x] 4.1 Пакет `packages/cms/licensing` (composer `cms/licensing` по образцу `cms/pay`, PSR-4 `Cms\Licensing\`, провайдер `Infrastructure/Providers/LicensingServiceProvider`: миграции, `routes/admin.php` + `routes/public.php`, морф-алиасы `organization`/`license_plan`); подключить в `apps/pay-service/composer.json`, каталог `../../packages/cms/licensing/tests` — в testsuite Packages `apps/pay-service/phpunit.xml` и в `pest()->extend(...)` в `apps/pay-service/tests/Pest.php` (иначе тесты пакета не исполняются вовсе); проверить `./tools/cms up pay` без ошибок и запуск пустого теста пакета
- [x] 4.2 Добавить `licensing` в `ArchitectureGateTest::modulePackages()` и `MODULES` в `tools/refactor-inventory.sh`; проверить `./tools/refactor-inventory.sh --strict` зелёный

## 5. Домен licensing: модели, enum, миграции

- [x] 5.1 Миграции `0004_01_01_*`: `licensing_organizations` (name, contact_first_name, contact_last_name, phone, email, telegram, activity, employees_count, usage_purpose, project_id), `license_plans` (code, name, price_minor/currency/interval — nullable-тройка, project_id; unique project+code), `license_plan_features` (plan_id, organization_id nullable, code, name, project_id; unique plan+organization+code), `licenses` (uuid PK, organization_id, plan_id, key unique, signed_payload text, issued_at, expires_at, revoked_at, project_id), `license_signing_keys` (project_id unique, public_key, secret_key encrypted); проверить накат/откат `./tools/cms migrate pay`
- [x] 5.2 Модели `Organization`, `Plan` (реализует `Subscribable` при заполненной цене; доменная ошибка при попытке подписки без цены), `PlanFeature`, `License`, `SigningKey` с `BelongsToProject`, фабриками, связями; enum `LicenseStatus` (Active/Expired/Revoked, `revoked_at` приоритетнее); unit-тесты статуса и валидации цены (тройка целиком)

## 6. Licensing: организации, планы, фичи (admin CRUD)

- [x] 6.1 CRUD организаций: FormRequests (обязательные name/contact_first_name/contact_last_name/email), `DTOs/Organization/UpsertOrganizationDTO` (Optional-семантика), Handlers (Upsert/Delete, List/Show `*Query`), Resource, маршруты `/api/admin/v1/projects/{project}/pay/licensing/organizations` (middleware по образцу pay: AuthorizeOperator + EnsureServiceEnabled:pay) под `pay.licensing.view|manage`; удаление организации с лицензиями → `OrganizationRuleViolation`; feature-тесты: CRUD, пагинация, 403, tenant-изоляция, защита удаления
- [x] 6.2 CRUD планов и фич: `DTOs/Plan/UpsertPlanDTO` (цена — вся тройка или ничего), `DTOs/PlanFeature/UpsertPlanFeatureDTO`, уникальность code в проекте и plan+organization+code, расчёт эффективного набора фич (базовые NULL + переопределения) в Domain, удаление плана с лицензиями → `PlanRuleViolation`; feature-тесты: уникальности, эффективный набор, частичная цена 422, защита удаления

## 7. Licensing: криптосхема и жизненный цикл лицензий

- [x] 7.1 Подпись: репозиторий ключевой пары в `Infrastructure/Persistence` (ленивая генерация `sodium_crypto_sign_keypair`, secret через `Crypt`), порт `Domain/Contracts/LicenseSigner` + Ed25519-реализация; admin-эндпоинт публичного ключа (`pay.licensing.manage`); тесты: пара создаётся при первом обращении, приватный ключ отсутствует во всех API-ответах
- [x] 7.2 Выпуск: генератор ключа `LIC-XXXXX-…` (Crockford Base32, `random_bytes`, unique), `IssueLicenseCommand`+Handler (payload: license_id, key, organization, plan code, эффективные фичи, issued_at, expires_at; подпись) и HTTP-слой выпуска — `POST .../pay/licensing/licenses` (FormRequest: organization_id, plan_id, expires_at; право `pay.licensing.manage`, Resource), tenant-изоляция организации/плана; feature-тесты: выпуск через эндпоинт, 403 без права, фиксация фич на момент выпуска, изоляция
- [x] 7.3 Файл и отзыв: скачивание конверта `{data, signature}` (`pay.licensing.manage`); `RevokeLicenseCommand`+Handler (необратимо, повтор → `LicenseRuleViolation`); список/просмотр лицензий (`pay.licensing.view`, фильтры по организации и статусу через laravel-query-builder); тесты: `sodium_crypto_sign_verify_detached` проходит, изменённый байт `data` роняет проверку, повторный отзыв 422, фильтры
- [x] 7.4 Публичная валидация `POST /api/v1/pay/licensing/validate` (`routes/public.php`; pay-префикс обязателен — gateway маршрутизирует только `/api/v1/pay/*`): вход только `key`, резолв проекта по ключу, для `active` — статус/plan code/фичи/expires_at без PII, иначе единый `invalid`; throttle (без AuthorizeProjectKey — ключ и есть аутентификация); feature-тесты: идентичность трёх невалидных ответов, состав валидного, rate limit

## 8. Интеграция подписка ⇄ лицензия

- [x] 8.1 Листенеры licensing (идемпотентные): `SubscriptionStarted` (subject `license_plan` + subscriber `organization` → выпуск лицензии с `expires_at = periodEndsAt`; при существующей неотозванной, в т.ч. истёкшей, лицензии плана — продление, не дубль), `SubscriptionPeriodExtended` (перевыпуск payload с новым `expires_at`, ключ прежний; отозванная лицензия игнорируется — остаётся `revoked`); регистрация в LicensingServiceProvider; unit-тесты обоих листенеров: идемпотентность, «чужие» события (site_user/plan — игнор), отозванная лицензия не воскресает
- [x] 8.2 Сквозной feature-тест цикла + admin-оформление с licensing-сущностями: оформление подписки организации на лицензионный план с `provider=manual` → лицензия выпущена со сроком периода → 422 на лицензионный план без цены → 422 на организацию чужого проекта → manual-подтверждение платежа продления → период сдвинут И лицензия перевыпущена (новый `expires_at`, прежний `key`, валидная подпись) → отмена подписки лицензию не меняет → отзыв лицензии подписку не меняет

## 9. Права, Swagger, качество

- [x] 9.1 `PayManifest`: `pay.licensing.view|manage` (группа `licensing`); feature-тест реального permission-механизма (роль с правом / 403 без)
- [x] 9.2 Swagger-аннотации всех новых/изменённых эндпоинтов, пересборка `./tools/cms api`; RouteCoverageTest pay-service: характеризационные снимки всех новых licensing-маршрутов (~15: CRUD организаций/планов/фич, выпуск/отзыв/список лицензий, файл, публичный ключ, validate) и пары в `payRouteCoverageCoveredPairs()` — гейт guard 0.3 роняет сьюту при маршруте без снимка; проверить маршруты `licensing/*` и admin-оформление в openapi.json, зелёные OpenApiContractTest всех четырёх сервисов
- [x] 9.3 Финальный прогон: `composer lint`, `composer stan` (level 8), Pest пакетов pay/licensing/shared/contracts и всех сервисов, `ArchitectureGateTest`, `./tools/refactor-inventory.sh --strict`, полный `tools/smoke.sh`; summary изменения по `.ai/skills/documentation`
