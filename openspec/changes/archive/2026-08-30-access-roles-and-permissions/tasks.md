# Tasks: access-roles-and-permissions

## 1. Команда синхронизации

- [x] 1.1 Handler синхронизации в `packages/cms/auth/src/Application/Handlers/`: обход всех записей `service_manifests`, upsert прав через существующий `PermissionCatalog`, раскрытие системных ролей по всем проектам через `SystemRoleSyncer`, сбор счётчиков (добавлено, обновлено, ролей пересобрано, осиротело); проверить на двух проектах — состав ролей совпадает с шаблонами
- [x] 1.2 Консольная команда `permissions:sync` в `packages/cms/auth/src/Console/`: вызов handler, вывод сводки таблицей, флаг удаления осиротевших прав; проверить два запуска подряд — второй показывает нулевые изменения
- [x] 1.3 Осиротевшие права: без флага — только перечисление в сводке; с флагом — удаление права и его привязок к ролям, включая кастомные; проверить обе ветки на праве, вручную заведённом в каталоге
- [x] 1.4 Сброс кэшей после синхронизации: кэш прав spatie, версия bootstrap-кэша, оповещение сервисов вниз по стеку — тем же способом, что в `UpdateRolePermissionsHandler`; проверить `/api/admin/v1/bootstrap` сразу после команды — новый набор прав виден без перелогина
- [x] 1.5 `permissions:sync` в бутстрапе: `infra/docker/entrypoint.sh` и `tools/smoke.sh` после публикации манифестов; проверить `./tools/cms bootstrap` на чистом стеке — роли проектов заполнены

## 2. Системные роли под сервисы платформы

- [x] 2.1 Шаблоны в `packages/cms/auth/config/cms-auth.php`: `billing` = `pay.*`, `licensing` = `licensing.*`, существующие `owner`, `admin`, `editor`, `analyst`, `viewer` без изменений; проверить `permissions:sync` на существующем проекте — новые роли созданы, старые не потеряли прав
- [x] 2.2 Тест раскрытия ролей: роль сервиса содержит только права своего сервиса, `viewer` — только `*.view`, `super-admin` остаётся глобальной и вне проектных ролей; проверить Pest auth-сервиса
- [x] 2.3 Проверка доступа по роли сервиса: участник с ролью `billing` открывает разделы оплаты и получает отказ в контенте, аналитике и управлении доступом; проверить тестом на middleware `RequirePermission`

## 3. Каталог прав проекта

- [x] 3.1 Query каталога прав в `Application/Queries/`: чтение прав guard `admin` с меткой и группой, вывод сервиса из префикса ключа, отбор по включённым сервисам проекта; проверить на проекте с выключенным `pay` — прав `pay.*` в ответе нет
- [x] 3.2 Маршрут `GET /api/admin/v1/projects/{project}/permissions` с правом `auth.roles.view`, контроллер и `JsonResource` в общем envelope; проверить ответ и отказ 403 без права
- [x] 3.3 Состав роли отдаётся полностью и при выключенном сервисе: проверить чтение роли, содержащей право выключенного сервиса
- [x] 3.4 `./tools/cms api` — новый маршрут в едином swagger

## 4. Консоль: вкладка «Роли» в разделе «Команда»

- [x] 4.1 Вкладки «Участники» / «Роли» в `frontends/admin/src/components/pages/team`: компоненты вкладок из склада `source-admin`, вкладка ролей — по праву `auth.roles.view`; проверить в браузере на операторе без права — вкладки ролей нет, участники работают
- [x] 4.2 Слой данных: запросы списка ролей и каталога прав в `src/lib/admin`, хук раздела рядом с `use-team-page`; проверить, что список ролей и каталог приходят из платформы, моков нет
- [x] 4.3 Список ролей: карточка с названием, признаком системной роли и составом прав по группам; действия правки и удаления — только у кастомных ролей и только с правом `auth.roles.manage`; проверить обе роли в браузере
- [x] 4.4 Диалог роли: поле названия и чекбоксы прав по группам каталога с подписями из каталога, при открытии существующей роли отмечены её права; проверить создание и правку — изменения видны в списке без перезагрузки
- [x] 4.5 Ошибка платформы в диалоге: занятое название роли показывается сообщением от API, роль не создаётся; проверить в браузере
- [x] 4.6 Выбор роли участника списком ролей проекта — в карточке участника и в приглашении вместо ввода названия; проверить, что созданная роль доступна для назначения сразу
- [x] 4.7 Зеркалирование: перенесённые из склада файлы совпадают побайтово, правки перенесены обратно в `source-admin`; проверить сверкой консоли со складом
- [x] 4.8 Тексты раздела — ключами `console.*` в реестре текстов, без осиротевших ключей; проверить реестр

## 5. Приёмка

- [x] 5.1 Backend-проверки: Pint, PHPStan level 8, Pest auth-сервиса, `./tools/refactor-inventory.sh --strict`
- [x] 5.2 Сценарий целиком на поднятом стеке: `permissions:sync` → создание кастомной роли в консоли → назначение участнику → участник видит ровно свои разделы; зафиксировать в e2e панели
- [x] 5.3 Регрессия доступа: существующие проекты и участники сохранили прежние роли и права после первого запуска команды; проверить до/после на `demo`
