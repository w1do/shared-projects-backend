# Tasks: admin-e2e-playwright

## 1. Установка и конфигурация

- [x] 1.1 Завести самодостаточный пакет `e2e/` с `@playwright/test` в devDependencies и убедиться, что установка проходит; `frontends/admin/package.json` и `bun.lock` не трогать
- [x] 1.2 Создать `playwright.config.ts` в корне: `testDir: e2e`, `baseURL` из `E2E_BASE_URL` со значением `http://localhost:8080`, проект Chromium, `trace: on-first-retry`, `video: retain-on-failure`, `screenshot: only-on-failure`, вывод в `e2e/.artifacts/`; проверить командой `npx playwright test --list` — конфиг читается, сценарии находятся
- [x] 1.3 Включить headed-режим по переменной `E2E_HEADED=1` вместе с замедлением шагов; проверить, что без переменной прогон headless, а с ней открывается окно браузера
- [x] 1.4 Добавить `e2e/.artifacts/` в `.gitignore` и проверить `git status` — артефакты прогона не попадают в индекс

## 2. Предпосылки сценариев

- [x] 2.1 Реализовать вход оператора как переиспользуемую предпосылку с сохранением `storageState`, включающего **и cookies, и localStorage**; проверить, что сохранённое состояние содержит `console_sections` в обоих хранилищах, а не только cookie
- [x] 2.2 Реализовать помощник изменения состава сервисов проекта через API платформы с восстановлением исходного значения в `finally`; проверить тестом-образцом: после намеренного падения сценария сервис возвращён во включённое состояние
- [x] 2.3 Реализовать проверку готовности стека перед прогоном (gateway отвечает, панель отдаёт `/login`) с внятным сообщением, если стек не поднят; проверить запуском при остановленном стеке — сообщение понятное, а не таймаут селектора

## 3. Запуск и CI

- [x] 3.1 Добавить команду `./tools/cms e2e [--headed]`; проверить, что оба режима запускаются и что команда предупреждает о необходимости перезапустить контейнер панели после правок в `frontends/admin`
- [x] 3.2 Добавить задачу в `.github/workflows/pull-request.yml`: поднять стек, установить только Chromium (`--with-deps chromium`), выполнить сценарии headless, приложить артефакты при падении; проверить, что задача описана корректно и не ломает существующие задачи
- [x] 3.3 Прогнать полный набор дважды подряд без пересоздания окружения и убедиться, что второй прогон зелёный — состояние проекта восстанавливается

## 4. Документация

- [x] 4.1 Описать в `docs/admin-console.md`: как запускать сценарии локально (в том числе headed), где лежат артефакты, и что после правок панели контейнер нужно перезапустить; проверить, что описанные команды выполняются как написано
