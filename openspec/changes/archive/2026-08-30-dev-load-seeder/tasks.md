# Tasks: dev-load-seeder

## 1. Оркестратор и владение данными

- [x] 1.1 Подкоманда `./tools/cms seed-load [N]` (по умолчанию 20): guard на `APP_ENV=production`, вызов сеялок сервисов по очереди через `db:seed --class=Database\Seeders\LoadSeeder`, передача `LOAD_PROJECT_IDS` и `LOAD_PROJECTS`, сводка времени и счётчиков по сервисам; проверить `./tools/cms seed-load 1` — проходит целиком и печатает сводку
- [x] 1.2 auth-сеялка `apps/auth-service/database/seeders/LoadSeeder.php`: проекты с ключами `load-01…load-NN` (локали ru,en), удаление своих проектов и связанных данных перед посевом, вывод списка `project_id` в формате, который читает оркестратор; проверить повторный прогон — число проектов не растёт, проект `demo` на месте

## 2. Наполнение auth

- [x] 2.1 На каждый проект: включённые сервисы, заполненные настройки всех сервисов, публичный и секретный API-ключи, участники с разными ролями, записи аудита; проверить через `/api/admin/v1/bootstrap` — в проекте видны сервисы и права, разделы консоли непустые
- [x] 2.2 Пользователи сайта проекта батч-вставкой (детерминированный генератор, чанки 2000); проверить количество в БД и отклик списка `/api/admin/v1/projects/load-01/users` с пагинацией
- [x] 2.3 Кастомные роли проекта с разными наборами прав из каталога прав и распределение участников по системным и кастомным ролям; проверить входом под участником с ролью «только контент» — разделы оплаты и аналитики закрыты. Опора — каталог прав из `access-roles-and-permissions`; пока его нет, шаг пропускается с причиной в сводке

## 3. Наполнение content

- [x] 3.1 content-сеялка: дерево категорий через модель (nested set корректен), посты в разных статусах со слагами и привязкой к категориям, словарь переводов по локалям батч-вставкой; проверить `/api/admin/v1/projects/load-01/content/posts` и дерево категорий в консоли
- [x] 3.2 Медиа: несколько десятков небольших PNG на проект на диск `s3` с регистрацией `media_files`; проверить, что файл открывается через gateway по `/media/...` и виден в разделе медиа
- [x] 3.3 Города проекта: включённость нескольких десятков городов справочника и заполненное SEO у каждого; проверить раздел городов в консоли и выдачу включённых городов публичным API. Опора — справочник и включённость из `content-cities-and-regions`; пока их нет, шаг пропускается с причиной в сводке

## 4. Наполнение pay и licensing

- [x] 4.1 Тарифы, подписки в разных состояниях (active/canceled/paused) и 20 000 платежей с транзакциями на проект батч-вставкой чанками; проверить: время шага в сводке, связность (у платежа существуют плательщик, тариф, подписка), отклик списка платежей в консоли
- [x] 4.2 Лицензирование: организации, планы с фичами, релизы, лицензии и установки на проект; проверить раздел «Лицензирование» в консоли и выдачу списка лицензий с фильтрами

## 5. Наполнение analytics

- [x] 5.1 Батч-вставка событий прямо в ClickHouse (чанки 50 тыс.): 100 000 событий на проект за последний год, включая просмотры страниц и события платежей с суммами; проверить `overview`, `top-pages` и `revenue` за произвольный период — ряды непустые, выручка ненулевая
- [x] 5.2 Очистка аналитики своих проектов: мутации `DELETE` по `events` и по каждому материализованному представлению с `mutations_sync=1`; проверить повторный прогон — счётчики строк в ClickHouse совпадают с первым прогоном

## 6. Приёмка и документация

- [x] 6.1 Полный прогон на чистом стеке: `./tools/cms seed-load` укладывается в единицы минут, память процесса не исчерпывается, все разделы консоли непустые; зафиксировать фактические тайминги и объёмы
- [x] 6.2 Повторяемость и изоляция: второй прогон подряд даёт те же объёмы; вручную созданный проект и `demo` не затронуты; `./tools/cms seed-load 0` полностью убирает данные сидера во всех четырёх сервисах
- [x] 6.3 Пропуск нереализованных шагов: на стенде без справочника городов прогон завершается успешно, а сводка называет пропущенный шаг и причину
- [x] 6.4 Отказ в production: вызов сеялки при `APP_ENV=production` завершается сообщением без изменения данных (проверить обе точки — `tools/cms` и прямой `db:seed`)
- [x] 6.5 Регрессия существующих проверок на засеянном стенде: `tools/smoke.sh` и `./tools/cms e2e` зелёные (сидер не ломает `demo`-сценарии)
- [x] 6.6 Раздел в `docs/` о нагруженном стенде: команда, объёмы и время прогона, требования к диску, возврат к чистому состоянию; ссылка из README
