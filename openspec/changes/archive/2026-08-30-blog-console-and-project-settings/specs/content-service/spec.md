## MODIFIED Requirements

### Requirement: Посты

Admin API SHALL поддерживать CRUD постов проекта: заголовок, slug, контент, локаль, автор, привязка к категориям, обложка и баннер — ссылки на медиа-файлы того же проекта, признак закрепления `is_featured`, статусы `draft → scheduled → published → archived` (недопустимые переходы отклоняются), отложенная публикация по времени. Обложка и баннер MAY быть пустыми и MUST ссылаться только на медиа текущего проекта; ссылка на медиа другого проекта или на несуществующий файл SHALL отклоняться. Admin и публичное API SHALL отдавать адреса изображений поста ссылками платформы. Закреплённый пост в проекте MUST быть не более одного: сохранение поста с `is_featured` SHALL снимать признак с прежнего закреплённого поста того же проекта. Каждое сохранение SHALL создавать ревизию с возможностью восстановления. Admin API SHALL поддерживать удаление ревизии поста; удаление ревизии MUST NOT изменять пост, а ревизия другого поста или другого проекта SHALL отклоняться.

#### Scenario: Отложенная публикация

- WHEN пост в статусе scheduled со временем в будущем
- THEN после наступления времени он публикуется автоматически и появляется в публичном API

#### Scenario: Восстановление из ревизии

- WHEN оператор восстанавливает пост из ревизии
- THEN контент равен снимку, создаётся новая ревизия

#### Scenario: Изображения поста в ответе

- WHEN клиент запрашивает пост с заданной обложкой
- THEN ответ содержит ссылку платформы на изображение обложки

#### Scenario: Чужое медиа

- WHEN пост сохраняется со ссылкой на медиа другого проекта
- THEN запрос отклоняется ошибкой валидации, пост не изменяется

#### Scenario: Закреплённый пост один

- WHEN сохраняется пост с `is_featured`, а в проекте уже есть закреплённый пост
- THEN признак снимается с прежнего поста, закреплённым остаётся только сохранённый

#### Scenario: Удаление ревизии

- WHEN оператор удаляет ревизию поста
- THEN ревизия исчезает из истории, содержимое поста не меняется

#### Scenario: Ревизия чужого поста

- WHEN запрашивается удаление ревизии, принадлежащей другому посту или проекту
- THEN запрос отклоняется, ревизия сохраняется

## ADDED Requirements

### Requirement: Пересборка поста через AI

Admin API SHALL поддерживать запуск пересборки существующего поста: заголовок, блоки содержимого и SEO-поля собираются заново по материалам базы знаний проекта, число блоков MUST NOT превышать десяти. Slug, категории, теги, изображения и статус поста MUST оставаться прежними. Прежнее содержимое MUST сохраняться ревизией до записи нового. Пересборка SHALL выполняться в очереди и MUST быть представлена задачей в реестре фоновых задач проекта: принята, этап выполнения, исход. Отказ пересборки MUST оставлять пост неизменным, а причина MUST быть видна в реестре задач. Маршрут SHALL быть закрыт правом управления постами.

#### Scenario: Запуск пересборки

- WHEN оператор запускает пересборку поста
- THEN запрос принимается, в реестре задач проекта появляется задача пересборки этого поста

#### Scenario: Результат пересборки

- WHEN задача пересборки завершилась успешно
- THEN у поста новые заголовок, блоки и SEO, прежние slug, категории, теги, изображения и статус, а прежнее содержимое доступно ревизией

#### Scenario: Отказ пересборки

- WHEN пересборка завершилась ошибкой
- THEN пост не изменяется, задача в реестре отмечена отклонённой с причиной

#### Scenario: Нет права на посты

- WHEN пересборку запрашивает оператор без права управления постами
- THEN запрос отклоняется, задача не создаётся
