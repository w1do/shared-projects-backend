## 1. Инфраструктура и зависимости

- [x] 1.1 Добавить сервис `qdrant` в `infra/compose/compose.yaml` (образ `qdrant/qdrant`, том `qdrant-data`, сеть `platform`, healthcheck, без `ports:` и без маршрута в gateway) и dev-переопределение в `compose.dev.yaml`; проверка — `./tools/cms up`, контейнер `qdrant` в состоянии healthy, `curl http://qdrant:6333/readyz` из `content-service` отвечает 200, снаружи порт недоступен
- [x] 1.2 Добавить переменные окружения `QDRANT_URL`, `QDRANT_API_KEY`, `SERPAPI_KEY`, `SERPAPI_BASE_URL`, `CMS_AI_EMBEDDING_MODEL`, `CMS_AI_EMBEDDING_DIMENSION`, `RESEARCH_*` в `infra/compose/.env.example` и в env-блок content-сервисов; проверка — `php artisan config:show cms-ai` и `config:show cms-research` в контейнере показывают значения из окружения
- [x] 1.3 Установить `spatie/laravel-tags` в `apps/content-service/composer.json`; проверка — `composer install` проходит, `php artisan vendor:publish --tag=tags-migrations` доступен

## 2. Контракт AI: эмбеддинги, произвольная схема, темы

- [x] 2.1 Добавить в `packages/cms/ai/config/cms-ai.php` модель и размерность эмбеддингов из окружения; проверка — значения читаются в `AiProviderConfig`, юнит-тест на дефолты зелёный
- [x] 2.2 Реализовать `Cms\Ai\Infrastructure\Ai\JsonSchemaCompiler`: трансляция поддерживаемого подмножества JSON Schema (`object`, `array`, `string`, `integer`, `number`, `boolean`, `nullable`, `required`, `description`, вложенность) в `callable(JsonSchema): array`; проверка — юнит-тесты покрывают вложенный объект, массив объектов, nullable-поле и отказ на неподдерживаемой конструкции (`oneOf`, `$ref`) до обращения к провайдеру
- [x] 2.3 Добавить в контракт `AiOperations` операцию `embed(EmbedRequestDTO): EmbedResultDTO` и `embeddingDimension(): int` с DTO в `Application/DTOs/Embed/`; проверка — реализация `LaravelAiOperations` через `Laravel\Ai\Embeddings` на экземпляре провайдера `cms-ai`, тест на фейке SDK: три текста → три вектора в исходном порядке, пустой набор → пустой результат без вызова провайдера
- [x] 2.4 Добавить в контракт `AiOperations` операцию `runInstruct(RunInstructRequestDTO): RunInstructResultDTO` (правило + JSON Schema + входные данные → разобранный ответ) с DTO в `Application/DTOs/RunInstruct/`; проверка — тест: ответ по схеме разбирается, ответ не по схеме даёт `AiResponseException`, некорректная схема даёт ошибку до сети
- [x] 2.5 Добавить в контракт `AiOperations` операцию `extractTopics(ExtractTopicsRequestDTO): TopicListDTO` (запрос + материалы + число тем → заголовок, пояснение, предложенная категория) с промптом в `PromptCatalog` и схемой в `ResponseSchemas`; проверка — тест на фейке SDK возвращает список тем, при недостатке материала — меньшее число тем без выдуманных
- [x] 2.6 Обновить тестовые заглушки `AiOperations` в `packages/cms/content/tests` и `packages/cms/localization/tests` под расширенный контракт; проверка — `composer test` в content-service и localization-пакете зелёный

## 3. Пакет `cms/instructs` по канону

- [x] 3.1 Пересоздать `packages/cms/instructs` под namespace `Cms\Instructs` с four-layer, `composer.json` (`cms/instructs`, зависимости `cms/ai`, `cms/shared`, `spatie/laravel-data`, `spatie/laravel-package-tools`) и провайдером в `Infrastructure/Providers/InstructsServiceProvider.php`; проверка — `./tools/refactor-inventory.sh --strict` не ругается на структуру пакета
- [x] 3.2 Написать миграции `instructs` (`project_id` nullable для системных, `key`, `title`, `category`, `rule`, `schema` json, `published`, `is_system`, softDeletes) и `instruct_usages` (`project_id`, `instruct_id`, снимки `title`/`category`/`rule`/`schema`, `nullableMorphs('generated')`); проверка — `./tools/cms migrate content` проходит, откат `down()` чистый
- [x] 3.3 Реализовать `Domain/Models/Instruct` (с `BelongsToProject`) и `Domain/Models/InstructUsage`, enum `Domain/Enums/InstructCategory` с пятью категориями платформы (`project_description`, `category_tree`, `post_topics`, `post_body`, `post_seo`); проверка — юнит-тест: скоуп по проекту не отдаёт чужие инструкции, системная инструкция видна всем проектам
- [x] 3.4 Реализовать `Application/Handlers/UpsertInstructHandler` и `DeleteInstructHandler` с DTO `Application/DTOs/Instruct/UpsertInstructDTO`; доменные отказы (некорректная схема, правка системной инструкции, категория вне перечня) — исключениями `*RuleViolation` в `Application/Exceptions/`; проверка — тесты на каждый отказ, сущность не изменяется
- [x] 3.5 Реализовать `Application/Actions/ResolveInstructAction` (опубликованная инструкция проекта нужной категории, иначе системная) и `RecordInstructUsageAction` (неизменяемый снимок правила и схемы); проверка — тест: правка инструкции после применения не меняет снимок, удаление инструкции сохраняет снимок
- [x] 3.6 Реализовать `Application/Queries/ListInstructsQuery` и `GetInstructQuery` со скоупом проекта; проверка — тест: инструкция другого проекта отдаётся как отсутствующая
- [x] 3.7 Реализовать HTTP-поверхность `Presentation/Http/Api/V1/` (контроллер, FormRequests, Resources) и `routes/admin.php` под `api/admin/v1/projects/{project}/instructs`, каждый маршрут закрыт правом `content.instructs.view|manage`; проверка — снимок-тест ответа списка и отказ 403 без права
- [x] 3.8 Написать идемпотентный сидер предустановленных системных инструкций для пяти категорий с правилом и схемой; проверка — двойной запуск сидера не создаёт дубликатов, `runInstruct` по каждой системной схеме проходит компилятор из 2.2
- [x] 3.9 Реализовать `Domain/Policies/InstructPolicy` (правка и удаление только своей инструкции проекта, системные — только чтение); проверка — тесты политики на все три случая

## 4. Пакет `cms/research`: каркас, схема, пайплайн

- [x] 4.1 Пересоздать `packages/cms/research` под namespace `Cms\Research` с four-layer, `composer.json` (`cms/ai`, `cms/content`, `cms/shared`, `spatie/laravel-data`) и провайдером в `Infrastructure/Providers/ResearchServiceProvider.php`; проверка — `./tools/refactor-inventory.sh --strict` чистый по новому пакету
- [x] 4.2 Написать миграции `researches`, `research_sources`, `research_topics`, `project_buildouts` с `project_id` и индексами из design (в т.ч. уникальный `(research_id, title)` для тем); проверка — `./tools/cms migrate content` проходит, откат чистый
- [x] 4.3 Реализовать модели `Research`, `ResearchSource`, `ResearchTopic`, `ProjectBuildout` с `BelongsToProject` и enum-ы `ResearchStatus`, `ResearchProgressStage`, `TopicStatus`, `SearchEngine` с переходами в методах enum; проверка — юнит-тесты на допустимые и недопустимые переходы статуса
- [x] 4.4 Перенести порты `SerpSearchClient` и `PageContentFetcher` в `Domain/Contracts/`, реализации `SerpApiSearchClient` и `HttpPageContentFetcher` — в `Infrastructure/`, сохранив SSRF-защиту и ограничение длины текста донора; проверка — перенесённые тесты `HttpPageContentFetcherTest` и `SerpApiSearchClientTest` зелёные на новых namespace
- [x] 4.5 Реализовать `Application/Actions/SplitPromptIntoSubQueriesAction` и `SummarizeSourcesAction` поверх `AiOperations` (вместо донорского `PolzaClient`); проверка — тесты на фейке `AiOperations`: промпт разбивается на заданное число подзапросов, сводный текст собирается по переданным источникам
- [x] 4.6 Реализовать `Application/Handlers/StartResearchHandler` (валидация предела одновременных прогонов, создание записи, постановка задачи) и `CancelResearchHandler` (идемпотентная отмена); проверка — тесты: превышение предела отклоняется без остановки идущих, повторная отмена не меняет состояние
- [x] 4.7 Реализовать `Infrastructure/Jobs/ProcessResearchJob` в очереди `research`: этапы, отмена на границе этапа, пропуск недоступных источников, отказ при нулевом числе доступных, идемпотентность повторной доставки, `failed()` → аудит; проверка — тесты: повтор завершённой задачи не меняет результат, отмена во время работы останавливает пайплайн, при всех недоступных источниках — неуспешное завершение с причиной
- [x] 4.8 Реализовать `Application/Queries/ListResearchesQuery` и `GetResearchQuery` со скоупом проекта; проверка — тест: исследование другого проекта отдаётся как отсутствующее
- [x] 4.9 Реализовать HTTP-поверхность ресёрча (контроллер, FormRequests, Resources) и маршруты `api/admin/v1/projects/{project}/research` под правами `content.research.view|run`; проверка — снимок-тесты списка, карточки и запуска, 403 без права

## 5. База знаний в Qdrant

- [x] 5.1 Объявить порт `Domain/Contracts/KnowledgeBase` (запись точек, поиск с фильтрами по категории и диапазону дат, удаление по проекту) с обязательным `ProjectId` в каждом методе; проверка — интерфейс покрыт in-memory фейком, используемым в тестах пакета
- [x] 5.2 Реализовать `Infrastructure/Persistence/QdrantKnowledgeBase` поверх REST API Qdrant: одна коллекция, обязательный фильтр по `project_id`, payload с обязательными полями (тема, запрос, контент, дата, категория) и детерминированный UUIDv5 идентификатор точки; проверка — тесты на `Http::fake`: запрос поиска всегда содержит фильтр по проекту, повторная запись формирует тот же идентификатор точки
- [x] 5.3 Реализовать идемпотентную консольную команду создания коллекции с размерностью из `cms-ai`; проверка — двойной запуск не пересоздаёт коллекцию, отсутствие Qdrant даёт понятную ошибку конфигурации
- [x] 5.4 Реализовать `Infrastructure/Jobs/IndexResearchJob`: после успешного ресёрча считает эмбеддинги источников и пишет точки, помечая `indexed_at`; недоступность Qdrant не портит результат ресёрча и приводит к повтору; проверка — тесты: при падении хранилища результат ресёрча остаётся доступен, повтор не создаёт дубликат, неуспешный и отменённый ресёрч не индексируются
- [x] 5.5 Отклонять запись точки без любого из обязательных метаданных с записью причины в журнал; проверка — тест: точка без категории не отправляется в Qdrant, причина в журнале
- [x] 5.6 Реализовать `Application/Queries/SearchKnowledgeQuery` (смысловой поиск с отбором по категории и датам); проверка — тест на фейке: материал соседнего проекта не попадает в выдачу, пустой результат не является ошибкой

## 6. Темы постов

- [x] 6.1 Реализовать `Application/Handlers/ExtractTopicsHandler` поверх `AiOperations::extractTopics` и материалов базы знаний; сопоставление предложенной категории с категориями проекта, иначе — сохранение предложенного названия; проверка — тесты: темы создаются по завершённому ресёрчу, по невершённому — отказ, существующая категория подхватывается
- [x] 6.2 Обеспечить идемпотентность повторного извлечения тем (уникальность `(research_id, title)`, сохранение `used`/`rejected`); проверка — тест: повтор добавляет только новые темы, состояния прежних не сбрасываются
- [x] 6.3 Реализовать смену состояния темы (`reject`, автоматический переход в `used` со ссылкой на пост) и отказ на повторное использование; проверка — тесты на каждый переход, ранее созданный пост не изменяется
- [x] 6.4 Реализовать запросы и HTTP-поверхность тем (`api/admin/v1/projects/{project}/research/{research}/topics`, `.../topics/{topic}`) под правами `content.topics.view|manage`; проверка — снимок-тесты ответа и 403 без права

## 7. Теги постов

- [x] 7.1 Опубликовать миграции `spatie/laravel-tags`, добавить `project_id` в `tags` и уникальность `(project_id, slug, type)`; проверка — `./tools/cms migrate content` проходит, откат чистый
- [x] 7.2 Реализовать модель `Cms\Content\Domain\Models\Tag` с `BelongsToProject`, прописать её в `config('tags.taggable.tag_model')`, подключить `HasTags` к `Post`; проверка — тест: одноимённые теги в двух проектах — разные записи, снятие тега в одном не влияет на другой
- [x] 7.3 Добавить теги во входные DTO и FormRequests постов и в `PostResource`; проверка — снимок-тест ответа поста содержит теги, повторное указание тега не плодит дубликатов
- [x] 7.4 Добавить отбор публичных постов по тегу в публичное API контента; проверка — тест: возвращаются только опубликованные посты проекта с этим тегом

## 8. Генерация поста по теме

- [x] 8.1 Реализовать `Application/Handlers/GeneratePostFromTopicHandler`: подбор материалов темы из базы знаний, применение инструкций категорий `post_body` и `post_seo`, отказ при отсутствии материала; проверка — тесты: пост создаётся черновиком, при пустой выдаче базы знаний — отказ без создания поста
- [x] 8.2 Заполнять созданному посту категории (с созданием отсутствующей предложенной), теги и `SeoMeta` (заголовок, описание, ключевые слова); проверка — тест: у сгенерированного поста заполнены категории, теги и SEO; отсутствующая категория создаётся
- [x] 8.3 Фиксировать снимок применённой инструкции на созданный пост через `RecordInstructUsageAction`; проверка — тест: у поста есть снимок с правилом и схемой на момент генерации
- [x] 8.4 Реализовать `Infrastructure/Jobs/GeneratePostJob` в очереди `research` с идемпотентностью и переводом темы в `used`; проверка — тесты: отказ генерации оставляет тему доступной и не создаёт поста, повтор задачи не создаёт второй пост
- [x] 8.5 Реализовать HTTP-поверхность запуска генерации (`POST api/admin/v1/projects/{project}/content/posts/generate`) под правом `content.posts.manage`; проверка — снимок-тест ответа запуска, 403 без права

## 9. Сборка проекта по AI

- [x] 9.1 Добавить в `cms/auth` миграцию полей `description` и `topic` у `projects`, обновить модель, DTO, FormRequests и `ProjectResource`; проверка — снимок-тест ответа проекта содержит описание и тематику, правка сохраняется
- [x] 9.2 Добавить внутренний эндпоинт обновления полей проекта в `packages/cms/auth/routes/internal.php` под `ServiceToken` и метод в `Cms\Shared\AuthClient\AuthClient`; проверка — тест: вызов без сервисного токена отклоняется, с токеном обновляет описание и тематику
- [x] 9.3 Реализовать `Application/Handlers/StartProjectBuildoutHandler` (отказ при уже выполняющейся сборке) и `Infrastructure/Jobs/BuildProjectJob`: применение инструкций `project_description` и `category_tree` через `runInstruct`; проверка — тесты: повторный запуск во время работы отклоняется, ответ не по схеме завершает сборку неуспешно без изменения проекта и категорий
- [x] 9.4 Реализовать применение результата: создание категорий одной транзакцией с сохранением вложенности и пропуском существующих по `slug`, затем обновление полей проекта; незаполненные поля не затираются; проверка — тесты: ошибка на середине списка не оставляет части дерева, повтор после частичного применения не дублирует категории и дозаполняет проект, соседний проект не изменяется
- [x] 9.5 Реализовать HTTP-поверхность сборки (`POST .../buildout`, `GET .../buildout`) под правом `auth.projects.manage`; проверка — снимок-тесты запуска и состояния, 403 без права

## 10. Манифест, права, локализация

- [x] 10.1 Добавить в `ContentManifest` права `content.research.view|run`, `content.topics.view|manage`, `content.instructs.view|manage` и пункты навигации разделов ресёрча и инструкций; проверка — `php artisan manifest:publish` проходит, права и разделы видны в bootstrap-ответе консоли
- [x] 10.2 Зарегистрировать ключи локализации новых разделов и подписей в `Cms\Contracts\Localization\ContentLocalizationKeys` и словаре; проверка — тест перечисления ключей зелёный, консоль не показывает сырых ключей
- [x] 10.3 Подключить провайдеры `cms/research` и `cms/instructs` в `apps/content-service` (`composer.json`, path-репозиторий); проверка — `php artisan package:discover` находит оба, маршруты видны в `route:list`

## 11. Удаление доноров

- [x] 11.1 Удалить `packages/cms/serp-research` и остатки донорских файлов `Botsync\*`/`PolzaAi\*` из `packages/cms/research` и `packages/cms/instructs`; проверка — `grep -r "Botsync\|PolzaAi\|botsync/\|polzaai/" packages/ apps/ --include=*.php --include=*.json` не даёт совпадений
- [x] 11.2 Прогнать архитектурный гейт и инвентарь с новыми пакетами в перечне модуль-пакетов; проверка — `packages/cms/shared/tests/ArchitectureGateTest.php` зелёный после добавления `research` и `instructs` в `modulePackages()`, `./tools/refactor-inventory.sh --strict` чистый

## 12. Консоль: дашборд и карточка проекта

- [x] 12.1 Добавить чтение текущего проекта (id, название, описание, тематика) в слой данных консоли `lib/admin/data-source/platform/`; проверка — юнит-тест маппера, экран не использует mock-данные
- [x] 12.2 Добавить секцию карточки проекта на дашборд с копированием идентификатора и подсказкой при незаполненном описании; экран добавляется одновременно в `frontends/admin` и `frontends/source-admin`; проверка — `diff -r frontends/admin/src/components frontends/source-admin/src/components` пуст, карточка показывает данные выбранного проекта
- [x] 12.3 Добавить правку названия и описания из карточки, скрытую без права управления проектом; проверка — изменение переживает перезагрузку, без права элементы правки не отрисовываются
- [x] 12.4 Добавить действие «Собрать проект по AI» с запросом тематики, показом хода, блокировкой повторного запуска и обновлением карточки по завершении; проверка — при выполняющейся сборке кнопка недоступна, по завершении показывается число созданных категорий, при ошибке — сообщение и возможность повтора

## 13. Консоль: разделы ресёрча и инструкций

- [x] 13.1 Добавить раздел ресёрча: список с отбором по состоянию, форма запуска, обновление этапа без перезагрузки, отмена; проверка — новое исследование появляется в списке без перезагрузки, этап обновляется, отмена переводит карточку в отменённое состояние
- [x] 13.2 Добавить карточку исследования: подзапросы, источники со ссылками, сводный текст; проверка — данные приходят из API платформы, mock-источник не используется
- [x] 13.3 Добавить на карточку исследования извлечение тем, список тем с предложенной категорией, действия «написать пост» и «отклонить», отличимые состояния тем; проверка — использованные темы ведут на созданный пост, отклонённые не предлагаются
- [x] 13.4 Добавить раздел инструкций: список с отбором по категории и происхождением, форма создания и правки с проверкой JSON схемы до отправки, публикация и снятие с публикации; проверка — некорректный JSON блокирует отправку, предустановленные инструкции открываются только на чтение с предложением создать свою
- [x] 13.5 Добавить в создание поста выбор темы и запуск генерации с показом хода; проверка — по завершении открывается созданный черновик с категориями, тегами и SEO
- [x] 13.6 Синхронизировать все новые экраны с `frontends/source-admin`; проверка — `diff -r frontends/admin/src/components frontends/source-admin/src/components` и `diff -r frontends/admin/src/app frontends/source-admin/src/app` пусты

## 14. Качество и приёмка

- [x] 14.1 Прогнать `composer lint`, `composer stan` (Larastan level 8) и `composer test` во всех затронутых приложениях; проверка — все три команды зелёные
- [x] 14.2 Обновить сводный swagger и документацию новых эндпоинтов через аннотации контроллеров; проверка — `./tools/cms api` собирается без ошибок, новые маршруты присутствуют в спецификации
- [x] 14.3 Проверить сквозной сценарий на поднятом стеке: сборка проекта по AI → ресёрч по запросу → индексация в Qdrant → извлечение тем → генерация поста; проверка — созданный пост в состоянии черновика имеет категории, теги и заполненное SEO, а в коллекции Qdrant присутствуют точки этого проекта с обязательными метаданными
