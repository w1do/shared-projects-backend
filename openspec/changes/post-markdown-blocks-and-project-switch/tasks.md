## 1. Блоки поста в content-service

- [x] 1.1 Миграция в `packages/cms/content/database/migrations`: колонка `posts.blocks` (json, nullable) и перенос существующего `body` в один блок с ULID и пустым названием; проверить `./tools/cms migrate content-service` и откат, а после отката — что `body` у постов остался прежним
- [x] 1.2 Добавить `Application/DTOs/Post/PostBlockDTO.php` (`id`, `title`, `markdown`) и поле `blocks` в `Post` (cast `array`) и `PostDTO`; проверить Pest-тестом, что пост с блоками читается со своим порядком и идентификаторами
- [x] 1.3 Добавить `blocks` в `UpsertPostDTO` (`list|Optional`) и присвоение идентификаторов в `UpsertPostHandler`: блок без `id` получает ULID, переданный `id` сохраняется; проверить Pest-тестом, что идентификаторы не меняются при правке названия, текста и порядка блоков
- [x] 1.4 Собирать `body` из блоков в `UpsertPostHandler` (`## {title}` + markdown, блок без названия — только текст); проверить Pest-тестом, что переданный клиентом `body` игнорируется, а собранный соответствует блокам
- [x] 1.5 Валидация блоков в `UpsertPostRequest`: массив объектов, непустой `markdown`, `title` до 255, уникальность `id` в пределах поста; проверить Pest-тестом 422 на дубликате `id` и на блоке без текста
- [x] 1.6 Внести `blocks` в выборки `SnapshotRevisionHandler` и `RestoreRevisionHandler`; проверить Pest-тестом, что восстановление из ревизии возвращает состав, порядок и идентификаторы блоков
- [x] 1.7 Отдавать `blocks` в admin- и публичном API поста; обновить снимки `posts-*` и `public-post*` и добавить снимок поста с несколькими блоками
- [x] 1.8 Поднять версию кэша контента, чтобы публичный список отдавался в новой форме; проверить Pest-тестом, что ответ после смены версии содержит блоки
- [x] 1.9 Добавить `blocks` и `cover_media_id`/`banner_media_id` в OpenAPI-аннотации `PostController` (store/update) и проверить, что тело запроса описано

## 2. Генерация поста блоками

- [x] 2.1 Перевести схему предустановленной инструкции `post_body` в `SystemInstructCatalog` на `blocks` (массив `{title, markdown}`, `minItems: 10`) и переписать текст правила под разбиение на смысловые части; проверить unit-тестом состав схемы
- [x] 2.2 Привести пресет «Пост» в `SchemaPresetCatalog` к тому же составу и обновить тест соответствия пресета разбору; проверить, что `SchemaPresetsTest` зелёный
- [x] 2.3 Разбирать `blocks` в `GeneratePostFromTopicHandler` и отклонять ответ модели, где блоков меньше 10, доменным исключением с понятным текстом; проверить Pest-тестами создание поста с 10 блоками и отказ на коротком ответе
- [x] 2.4 Обновить снимок `posts-generate` и сидер предустановленных инструкций; проверить `./tools/cms test content` и что снимок отражает новую схему
- [x] 2.5 Требовать в правиле и при разборе развёрнутый пост: все подтемы материала и не менее 8000 символов текста блоков; проверить Pest-тестом отказ на коротком ответе и генерацией против живой модели

## 3. Счётчик событий проекта

- [x] 3.1 Добавить в слой данных консоли чтение последних 10 событий журнала проекта (`GET /projects/{project}/audit`) с мягкой обработкой отказа и отсутствия права; покрыть node-тестом маппера событий, включая пустой журнал и ошибку
- [x] 3.2 Добавить хук последних событий проекта на TanStack Query с ключом по проекту; проверить, что смена проекта перечитывает события, а отказ не роняет панель

## 4. Верхняя панель и окно проекта

- [x] 4.1 Заменить в `AdminTopbar` индикатор «сайт онлайн» и кнопку «Новый проект» одной кнопкой `Название (N)`; при недоступном журнале кнопка показывает только название — проверить в браузере оба случая
- [x] 4.2 Собрать окно проекта на компонентах вёрстки (`Dialog`, штатный список, `Badge`): список проектов оператора с отметкой текущего, последние 10 событий и пустое состояние журнала; зеркалировать `components/**` в `source-admin` и проверить `diff -rq`
- [x] 4.3 Провести переключение проекта через слой данных (`setProjectKey` → сброс кэша запросов → перечитывание bootstrap) и показать понятную ошибку при отказе; проверить в браузере переход в другой проект, возврат к прежнему и сохранение текущего проекта при отказе
- [x] 4.4 Перенести создание проекта в окно проекта и убрать отдельную кнопку из панели; проверить в браузере видимость действия под супер-админом и его отсутствие у обычного оператора при доступном окне
- [x] 4.5 Проверить в браузере, что оператор с единственным проектом видит окно с его событиями и без предложения переключиться

## 5. Разделы без скелетона

- [x] 5.1 Провести признак загрузки в панели подключённых разделов, чтобы пустое состояние показывалось только после прихода данных; покрыть node-тестом там, где признак живёт в хуке раздела
- [x] 5.2 Убрать полноэкранные скелетоны разделов категорий, блога и клиентов вместе с их каталогами `loading/`; проверить в браузере, что раздел появляется сразу и не мигает пустым состоянием
- [x] 5.3 Убрать скелетоны разделов ресёрча и инструкций (включая `ResearchListSkeleton` и скелетон списка инструкций); проверить в браузере оба раздела на медленном ответе
- [x] 5.4 Убрать скелетон дашборда; проверить в браузере, что карточки появляются в уже показанном разделе
- [x] 5.5 Зеркалировать все правки `components/**` в `source-admin` и убедиться, что `diff -rq frontends/admin/src/components frontends/source-admin/src/components` пуст

## 6. Форма поста: содержимое блоками

- [x] 6.1 Установить `markdown-text-editor` (`bun install`) и закоммитить пересчитанный `bun.lock`; проверить, что контейнер панели поднимается с `--frozen-lockfile`
- [x] 6.2 Добавить обёртку редактора `components/ui/inputs/markdown-editor.tsx` над нативным `textarea` с локальными типами пакета; проверить в браузере ввод разметки, её отображение при наборе и снятие редактора при размонтировании
- [x] 6.3 Перевести `blog-form-schema` на `contentBlocks: [{id?, title, markdown}]` и убрать поля автора; проверить node-тестами схемы, что пост без блоков допустим, а блок без текста отклоняется
- [x] 6.4 Пересобрать секцию «Содержимое»: список блоков с полем названия, markdown-редактором, добавлением и удалением блока; проверить в браузере добавление, правку и удаление блока
- [x] 6.5 Удалить секцию «Автор» из формы поста и её тексты из реестра; проверить в браузере, что форма не спрашивает автора, а у сохранённого поста автором значится текущий оператор
- [x] 6.6 Провести блоки через маппер и мутации постов (`postToArticle`, `mapArticle`, `toArticleBody`, `toPostBody`), сохраняя идентификаторы; покрыть node-тестами маппера круговое преобразование и проверить в браузере, что блоки переживают перезагрузку
- [x] 6.7 Добавить новые тексты секции содержимого и окна проекта в реестр `console.*`; проверить, что в затронутых разделах нет строк мимо реестра

## 7. Проверки и сборка

- [x] 7.1 Прогнать `pint --test` и `phpstan` в content- и auth-сервисах; убедиться, что кодстайл и Larastan level 8 проходят
- [x] 7.2 Прогнать `./tools/cms test content-service`; убедиться, что архитектурный гейт, снимки контракта и новые тесты зелёные
- [x] 7.3 Прогнать `./tools/refactor-inventory.sh --strict`; убедиться, что новые классы лежат по канону слоёв
- [x] 7.4 Прогнать node-тесты консоли и `npx tsc --noEmit`; убедиться, что типы и тесты слоя данных зелёные
- [x] 7.5 Актуализировать сценарии e2e под новую верхнюю панель и форму поста и прогнать `npx playwright test`; убедиться, что набор зелёный
- [x] 7.6 Пересобрать единый swagger (`./tools/cms api`); убедиться, что блоки поста попали в спецификацию
