import { AddProductLoadingState } from "@/components/pages/products/loading";

/** Route-level skeleton while SSR taxonomy data for the add form resolves. */
export default function AddProductLoading() {
  return <AddProductLoadingState />;
}
