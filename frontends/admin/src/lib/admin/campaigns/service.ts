/**
 * Compatibility re-export — prefer `@/lib/admin/services`.
 */
export {
  listCampaigns,
  getCampaignById,
  createCampaign,
  updateCampaign,
  deleteCampaign,
  rehydrateCampaigns,
  rehydrateMockCampaigns,
  getCampaignCapabilities,
} from "@/lib/admin/services";
