<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('{{snake}}_items', function (Blueprint $table): void {
            $table->id();
            // tenant-изоляция: project_id обязателен в каждой бизнес-таблице
            $table->string('project_id')->index();
            $table->string('title');
            $table->string('status', 32)->default('draft');
            $table->timestamps();

            $table->unique(['project_id', 'title']);
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('{{snake}}_items');
    }
};
